// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Synchronization is an object representing the database table.
type Synchronization struct {
	ClientId            string      `boil:"ClientId" json:"ClientId" toml:"ClientId" yaml:"ClientId"`
	LastDateExported    null.String `boil:"LastDateExported" json:"LastDateExported,omitempty" toml:"LastDateExported" yaml:"LastDateExported,omitempty"`
	LastDateImported    null.String `boil:"LastDateImported" json:"LastDateImported,omitempty" toml:"LastDateImported" yaml:"LastDateImported,omitempty"`
	LastSynchronization null.Bytes  `boil:"LastSynchronization" json:"LastSynchronization,omitempty" toml:"LastSynchronization" yaml:"LastSynchronization,omitempty"`

	R *synchronizationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L synchronizationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SynchronizationColumns = struct {
	ClientId            string
	LastDateExported    string
	LastDateImported    string
	LastSynchronization string
}{
	ClientId:            "ClientId",
	LastDateExported:    "LastDateExported",
	LastDateImported:    "LastDateImported",
	LastSynchronization: "LastSynchronization",
}

// Generated where

type whereHelpernull_Bytes struct{ field string }

func (w whereHelpernull_Bytes) EQ(x null.Bytes) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Bytes) NEQ(x null.Bytes) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Bytes) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Bytes) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }
func (w whereHelpernull_Bytes) LT(x null.Bytes) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Bytes) LTE(x null.Bytes) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Bytes) GT(x null.Bytes) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Bytes) GTE(x null.Bytes) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var SynchronizationWhere = struct {
	ClientId            whereHelperstring
	LastDateExported    whereHelpernull_String
	LastDateImported    whereHelpernull_String
	LastSynchronization whereHelpernull_Bytes
}{
	ClientId:            whereHelperstring{field: "\"Synchronization\".\"ClientId\""},
	LastDateExported:    whereHelpernull_String{field: "\"Synchronization\".\"LastDateExported\""},
	LastDateImported:    whereHelpernull_String{field: "\"Synchronization\".\"LastDateImported\""},
	LastSynchronization: whereHelpernull_Bytes{field: "\"Synchronization\".\"LastSynchronization\""},
}

// SynchronizationRels is where relationship names are stored.
var SynchronizationRels = struct {
}{}

// synchronizationR is where relationships are stored.
type synchronizationR struct {
}

// NewStruct creates a new relationship struct
func (*synchronizationR) NewStruct() *synchronizationR {
	return &synchronizationR{}
}

// synchronizationL is where Load methods for each relationship are stored.
type synchronizationL struct{}

var (
	synchronizationAllColumns            = []string{"ClientId", "LastDateExported", "LastDateImported", "LastSynchronization"}
	synchronizationColumnsWithoutDefault = []string{"ClientId", "LastDateExported", "LastDateImported", "LastSynchronization"}
	synchronizationColumnsWithDefault    = []string{}
	synchronizationPrimaryKeyColumns     = []string{"ClientId"}
)

type (
	// SynchronizationSlice is an alias for a slice of pointers to Synchronization.
	// This should generally be used opposed to []Synchronization.
	SynchronizationSlice []*Synchronization

	synchronizationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	synchronizationType                 = reflect.TypeOf(&Synchronization{})
	synchronizationMapping              = queries.MakeStructMapping(synchronizationType)
	synchronizationPrimaryKeyMapping, _ = queries.BindMapping(synchronizationType, synchronizationMapping, synchronizationPrimaryKeyColumns)
	synchronizationInsertCacheMut       sync.RWMutex
	synchronizationInsertCache          = make(map[string]insertCache)
	synchronizationUpdateCacheMut       sync.RWMutex
	synchronizationUpdateCache          = make(map[string]updateCache)
	synchronizationUpsertCacheMut       sync.RWMutex
	synchronizationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single synchronization record from the query.
func (q synchronizationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Synchronization, error) {
	o := &Synchronization{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for Synchronization")
	}

	return o, nil
}

// All returns all Synchronization records from the query.
func (q synchronizationQuery) All(ctx context.Context, exec boil.ContextExecutor) (SynchronizationSlice, error) {
	var o []*Synchronization

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Synchronization slice")
	}

	return o, nil
}

// Count returns the count of all Synchronization records in the query.
func (q synchronizationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count Synchronization rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q synchronizationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if Synchronization exists")
	}

	return count > 0, nil
}

// Synchronizations retrieves all the records using an executor.
func Synchronizations(mods ...qm.QueryMod) synchronizationQuery {
	mods = append(mods, qm.From("\"Synchronization\""))
	return synchronizationQuery{NewQuery(mods...)}
}

// FindSynchronization retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSynchronization(ctx context.Context, exec boil.ContextExecutor, clientId string, selectCols ...string) (*Synchronization, error) {
	synchronizationObj := &Synchronization{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"Synchronization\" where \"ClientId\"=$1", sel,
	)

	q := queries.Raw(query, clientId)

	err := q.Bind(ctx, exec, synchronizationObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from Synchronization")
	}

	return synchronizationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Synchronization) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no Synchronization provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(synchronizationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	synchronizationInsertCacheMut.RLock()
	cache, cached := synchronizationInsertCache[key]
	synchronizationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			synchronizationAllColumns,
			synchronizationColumnsWithDefault,
			synchronizationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(synchronizationType, synchronizationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(synchronizationType, synchronizationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"Synchronization\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"Synchronization\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into Synchronization")
	}

	if !cached {
		synchronizationInsertCacheMut.Lock()
		synchronizationInsertCache[key] = cache
		synchronizationInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Synchronization.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Synchronization) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	synchronizationUpdateCacheMut.RLock()
	cache, cached := synchronizationUpdateCache[key]
	synchronizationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			synchronizationAllColumns,
			synchronizationPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update Synchronization, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"Synchronization\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, synchronizationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(synchronizationType, synchronizationMapping, append(wl, synchronizationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update Synchronization row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for Synchronization")
	}

	if !cached {
		synchronizationUpdateCacheMut.Lock()
		synchronizationUpdateCache[key] = cache
		synchronizationUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q synchronizationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for Synchronization")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for Synchronization")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SynchronizationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), synchronizationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"Synchronization\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, synchronizationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in synchronization slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all synchronization")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Synchronization) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no Synchronization provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(synchronizationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	synchronizationUpsertCacheMut.RLock()
	cache, cached := synchronizationUpsertCache[key]
	synchronizationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			synchronizationAllColumns,
			synchronizationColumnsWithDefault,
			synchronizationColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			synchronizationAllColumns,
			synchronizationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert Synchronization, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(synchronizationPrimaryKeyColumns))
			copy(conflict, synchronizationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"Synchronization\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(synchronizationType, synchronizationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(synchronizationType, synchronizationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert Synchronization")
	}

	if !cached {
		synchronizationUpsertCacheMut.Lock()
		synchronizationUpsertCache[key] = cache
		synchronizationUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Synchronization record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Synchronization) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Synchronization provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), synchronizationPrimaryKeyMapping)
	sql := "DELETE FROM \"Synchronization\" WHERE \"ClientId\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from Synchronization")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for Synchronization")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q synchronizationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no synchronizationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from Synchronization")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for Synchronization")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SynchronizationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), synchronizationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"Synchronization\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, synchronizationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from synchronization slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for Synchronization")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Synchronization) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSynchronization(ctx, exec, o.ClientId)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SynchronizationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SynchronizationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), synchronizationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"Synchronization\".* FROM \"Synchronization\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, synchronizationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SynchronizationSlice")
	}

	*o = slice

	return nil
}

// SynchronizationExists checks if the Synchronization row exists.
func SynchronizationExists(ctx context.Context, exec boil.ContextExecutor, clientId string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"Synchronization\" where \"ClientId\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, clientId)
	}
	row := exec.QueryRowContext(ctx, sql, clientId)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if Synchronization exists")
	}

	return exists, nil
}
