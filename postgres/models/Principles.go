// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Principle is an object representing the database table.
type Principle struct {
	ID             int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	Principle      null.String `boil:"Principle" json:"Principle,omitempty" toml:"Principle" yaml:"Principle,omitempty"`
	SbjMatterIdxId null.Int    `boil:"SbjMatterIdxId" json:"SbjMatterIdxId,omitempty" toml:"SbjMatterIdxId" yaml:"SbjMatterIdxId,omitempty"`
	UpdatedAt      null.Time   `boil:"UpdatedAt" json:"UpdatedAt,omitempty" toml:"UpdatedAt" yaml:"UpdatedAt,omitempty"`

	R *principleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L principleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PrincipleColumns = struct {
	ID             string
	Principle      string
	SbjMatterIdxId string
	UpdatedAt      string
}{
	ID:             "Id",
	Principle:      "Principle",
	SbjMatterIdxId: "SbjMatterIdxId",
	UpdatedAt:      "UpdatedAt",
}

// Generated where

var PrincipleWhere = struct {
	ID             whereHelperint
	Principle      whereHelpernull_String
	SbjMatterIdxId whereHelpernull_Int
	UpdatedAt      whereHelpernull_Time
}{
	ID:             whereHelperint{field: "\"Principles\".\"Id\""},
	Principle:      whereHelpernull_String{field: "\"Principles\".\"Principle\""},
	SbjMatterIdxId: whereHelpernull_Int{field: "\"Principles\".\"SbjMatterIdxId\""},
	UpdatedAt:      whereHelpernull_Time{field: "\"Principles\".\"UpdatedAt\""},
}

// PrincipleRels is where relationship names are stored.
var PrincipleRels = struct {
}{}

// principleR is where relationships are stored.
type principleR struct {
}

// NewStruct creates a new relationship struct
func (*principleR) NewStruct() *principleR {
	return &principleR{}
}

// principleL is where Load methods for each relationship are stored.
type principleL struct{}

var (
	principleAllColumns            = []string{"Id", "Principle", "SbjMatterIdxId", "UpdatedAt"}
	principleColumnsWithoutDefault = []string{"Id", "Principle", "SbjMatterIdxId", "UpdatedAt"}
	principleColumnsWithDefault    = []string{}
	principlePrimaryKeyColumns     = []string{"Id"}
)

type (
	// PrincipleSlice is an alias for a slice of pointers to Principle.
	// This should generally be used opposed to []Principle.
	PrincipleSlice []*Principle

	principleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	principleType                 = reflect.TypeOf(&Principle{})
	principleMapping              = queries.MakeStructMapping(principleType)
	principlePrimaryKeyMapping, _ = queries.BindMapping(principleType, principleMapping, principlePrimaryKeyColumns)
	principleInsertCacheMut       sync.RWMutex
	principleInsertCache          = make(map[string]insertCache)
	principleUpdateCacheMut       sync.RWMutex
	principleUpdateCache          = make(map[string]updateCache)
	principleUpsertCacheMut       sync.RWMutex
	principleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single principle record from the query.
func (q principleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Principle, error) {
	o := &Principle{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for Principles")
	}

	return o, nil
}

// All returns all Principle records from the query.
func (q principleQuery) All(ctx context.Context, exec boil.ContextExecutor) (PrincipleSlice, error) {
	var o []*Principle

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Principle slice")
	}

	return o, nil
}

// Count returns the count of all Principle records in the query.
func (q principleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count Principles rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q principleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if Principles exists")
	}

	return count > 0, nil
}

// Principles retrieves all the records using an executor.
func Principles(mods ...qm.QueryMod) principleQuery {
	mods = append(mods, qm.From("\"Principles\""))
	return principleQuery{NewQuery(mods...)}
}

// FindPrinciple retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPrinciple(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*Principle, error) {
	principleObj := &Principle{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"Principles\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, principleObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from Principles")
	}

	return principleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Principle) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no Principles provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(principleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	principleInsertCacheMut.RLock()
	cache, cached := principleInsertCache[key]
	principleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			principleAllColumns,
			principleColumnsWithDefault,
			principleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(principleType, principleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(principleType, principleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"Principles\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"Principles\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into Principles")
	}

	if !cached {
		principleInsertCacheMut.Lock()
		principleInsertCache[key] = cache
		principleInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Principle.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Principle) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	principleUpdateCacheMut.RLock()
	cache, cached := principleUpdateCache[key]
	principleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			principleAllColumns,
			principlePrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update Principles, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"Principles\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, principlePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(principleType, principleMapping, append(wl, principlePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update Principles row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for Principles")
	}

	if !cached {
		principleUpdateCacheMut.Lock()
		principleUpdateCache[key] = cache
		principleUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q principleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for Principles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for Principles")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PrincipleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), principlePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"Principles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, principlePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in principle slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all principle")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Principle) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no Principles provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(principleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	principleUpsertCacheMut.RLock()
	cache, cached := principleUpsertCache[key]
	principleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			principleAllColumns,
			principleColumnsWithDefault,
			principleColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			principleAllColumns,
			principlePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert Principles, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(principlePrimaryKeyColumns))
			copy(conflict, principlePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"Principles\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(principleType, principleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(principleType, principleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert Principles")
	}

	if !cached {
		principleUpsertCacheMut.Lock()
		principleUpsertCache[key] = cache
		principleUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Principle record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Principle) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Principle provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), principlePrimaryKeyMapping)
	sql := "DELETE FROM \"Principles\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from Principles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for Principles")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q principleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no principleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from Principles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for Principles")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PrincipleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), principlePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"Principles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, principlePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from principle slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for Principles")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Principle) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPrinciple(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PrincipleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PrincipleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), principlePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"Principles\".* FROM \"Principles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, principlePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PrincipleSlice")
	}

	*o = slice

	return nil
}

// PrincipleExists checks if the Principle row exists.
func PrincipleExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"Principles\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if Principles exists")
	}

	return exists, nil
}
