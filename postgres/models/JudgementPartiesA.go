// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// JudgementPartiesA is an object representing the database table.
type JudgementPartiesA struct {
	ID          string      `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	PartyANames null.String `boil:"PartyANames" json:"PartyANames,omitempty" toml:"PartyANames" yaml:"PartyANames,omitempty"`

	R *judgementPartiesAR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L judgementPartiesAL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var JudgementPartiesAColumns = struct {
	ID          string
	PartyANames string
}{
	ID:          "Id",
	PartyANames: "PartyANames",
}

// Generated where

var JudgementPartiesAWhere = struct {
	ID          whereHelperstring
	PartyANames whereHelpernull_String
}{
	ID:          whereHelperstring{field: "\"JudgementPartiesA\".\"Id\""},
	PartyANames: whereHelpernull_String{field: "\"JudgementPartiesA\".\"PartyANames\""},
}

// JudgementPartiesARels is where relationship names are stored.
var JudgementPartiesARels = struct {
}{}

// judgementPartiesAR is where relationships are stored.
type judgementPartiesAR struct {
}

// NewStruct creates a new relationship struct
func (*judgementPartiesAR) NewStruct() *judgementPartiesAR {
	return &judgementPartiesAR{}
}

// judgementPartiesAL is where Load methods for each relationship are stored.
type judgementPartiesAL struct{}

var (
	judgementPartiesAAllColumns            = []string{"Id", "PartyANames"}
	judgementPartiesAColumnsWithoutDefault = []string{"Id", "PartyANames"}
	judgementPartiesAColumnsWithDefault    = []string{}
	judgementPartiesAPrimaryKeyColumns     = []string{"Id"}
)

type (
	// JudgementPartiesASlice is an alias for a slice of pointers to JudgementPartiesA.
	// This should generally be used opposed to []JudgementPartiesA.
	JudgementPartiesASlice []*JudgementPartiesA

	judgementPartiesAQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	judgementPartiesAType                 = reflect.TypeOf(&JudgementPartiesA{})
	judgementPartiesAMapping              = queries.MakeStructMapping(judgementPartiesAType)
	judgementPartiesAPrimaryKeyMapping, _ = queries.BindMapping(judgementPartiesAType, judgementPartiesAMapping, judgementPartiesAPrimaryKeyColumns)
	judgementPartiesAInsertCacheMut       sync.RWMutex
	judgementPartiesAInsertCache          = make(map[string]insertCache)
	judgementPartiesAUpdateCacheMut       sync.RWMutex
	judgementPartiesAUpdateCache          = make(map[string]updateCache)
	judgementPartiesAUpsertCacheMut       sync.RWMutex
	judgementPartiesAUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single judgementPartiesA record from the query.
func (q judgementPartiesAQuery) One(ctx context.Context, exec boil.ContextExecutor) (*JudgementPartiesA, error) {
	o := &JudgementPartiesA{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for JudgementPartiesA")
	}

	return o, nil
}

// All returns all JudgementPartiesA records from the query.
func (q judgementPartiesAQuery) All(ctx context.Context, exec boil.ContextExecutor) (JudgementPartiesASlice, error) {
	var o []*JudgementPartiesA

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to JudgementPartiesA slice")
	}

	return o, nil
}

// Count returns the count of all JudgementPartiesA records in the query.
func (q judgementPartiesAQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count JudgementPartiesA rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q judgementPartiesAQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if JudgementPartiesA exists")
	}

	return count > 0, nil
}

// JudgementPartiesAs retrieves all the records using an executor.
func JudgementPartiesAs(mods ...qm.QueryMod) judgementPartiesAQuery {
	mods = append(mods, qm.From("\"JudgementPartiesA\""))
	return judgementPartiesAQuery{NewQuery(mods...)}
}

// FindJudgementPartiesA retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindJudgementPartiesA(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*JudgementPartiesA, error) {
	judgementPartiesAObj := &JudgementPartiesA{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"JudgementPartiesA\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, judgementPartiesAObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from JudgementPartiesA")
	}

	return judgementPartiesAObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *JudgementPartiesA) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no JudgementPartiesA provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(judgementPartiesAColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	judgementPartiesAInsertCacheMut.RLock()
	cache, cached := judgementPartiesAInsertCache[key]
	judgementPartiesAInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			judgementPartiesAAllColumns,
			judgementPartiesAColumnsWithDefault,
			judgementPartiesAColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(judgementPartiesAType, judgementPartiesAMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(judgementPartiesAType, judgementPartiesAMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"JudgementPartiesA\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"JudgementPartiesA\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into JudgementPartiesA")
	}

	if !cached {
		judgementPartiesAInsertCacheMut.Lock()
		judgementPartiesAInsertCache[key] = cache
		judgementPartiesAInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the JudgementPartiesA.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *JudgementPartiesA) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	judgementPartiesAUpdateCacheMut.RLock()
	cache, cached := judgementPartiesAUpdateCache[key]
	judgementPartiesAUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			judgementPartiesAAllColumns,
			judgementPartiesAPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update JudgementPartiesA, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"JudgementPartiesA\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, judgementPartiesAPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(judgementPartiesAType, judgementPartiesAMapping, append(wl, judgementPartiesAPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update JudgementPartiesA row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for JudgementPartiesA")
	}

	if !cached {
		judgementPartiesAUpdateCacheMut.Lock()
		judgementPartiesAUpdateCache[key] = cache
		judgementPartiesAUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q judgementPartiesAQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for JudgementPartiesA")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for JudgementPartiesA")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o JudgementPartiesASlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), judgementPartiesAPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"JudgementPartiesA\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, judgementPartiesAPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in judgementPartiesA slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all judgementPartiesA")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *JudgementPartiesA) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no JudgementPartiesA provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(judgementPartiesAColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	judgementPartiesAUpsertCacheMut.RLock()
	cache, cached := judgementPartiesAUpsertCache[key]
	judgementPartiesAUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			judgementPartiesAAllColumns,
			judgementPartiesAColumnsWithDefault,
			judgementPartiesAColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			judgementPartiesAAllColumns,
			judgementPartiesAPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert JudgementPartiesA, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(judgementPartiesAPrimaryKeyColumns))
			copy(conflict, judgementPartiesAPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"JudgementPartiesA\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(judgementPartiesAType, judgementPartiesAMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(judgementPartiesAType, judgementPartiesAMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert JudgementPartiesA")
	}

	if !cached {
		judgementPartiesAUpsertCacheMut.Lock()
		judgementPartiesAUpsertCache[key] = cache
		judgementPartiesAUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single JudgementPartiesA record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *JudgementPartiesA) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no JudgementPartiesA provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), judgementPartiesAPrimaryKeyMapping)
	sql := "DELETE FROM \"JudgementPartiesA\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from JudgementPartiesA")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for JudgementPartiesA")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q judgementPartiesAQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no judgementPartiesAQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from JudgementPartiesA")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for JudgementPartiesA")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o JudgementPartiesASlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), judgementPartiesAPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"JudgementPartiesA\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, judgementPartiesAPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from judgementPartiesA slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for JudgementPartiesA")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *JudgementPartiesA) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindJudgementPartiesA(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *JudgementPartiesASlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := JudgementPartiesASlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), judgementPartiesAPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"JudgementPartiesA\".* FROM \"JudgementPartiesA\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, judgementPartiesAPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in JudgementPartiesASlice")
	}

	*o = slice

	return nil
}

// JudgementPartiesAExists checks if the JudgementPartiesA row exists.
func JudgementPartiesAExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"JudgementPartiesA\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if JudgementPartiesA exists")
	}

	return exists, nil
}
