// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDictionaries(t *testing.T) {
	t.Parallel()

	query := Dictionaries()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDictionariesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDictionariesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Dictionaries().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDictionariesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DictionarySlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDictionariesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DictionaryExists(ctx, tx, o.UUID)
	if err != nil {
		t.Errorf("Unable to check if Dictionary exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DictionaryExists to return true, but got false.")
	}
}

func testDictionariesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	dictionaryFound, err := FindDictionary(ctx, tx, o.UUID)
	if err != nil {
		t.Error(err)
	}

	if dictionaryFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDictionariesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Dictionaries().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDictionariesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Dictionaries().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDictionariesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	dictionaryOne := &Dictionary{}
	dictionaryTwo := &Dictionary{}
	if err = randomize.Struct(seed, dictionaryOne, dictionaryDBTypes, false, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}
	if err = randomize.Struct(seed, dictionaryTwo, dictionaryDBTypes, false, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = dictionaryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = dictionaryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Dictionaries().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDictionariesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	dictionaryOne := &Dictionary{}
	dictionaryTwo := &Dictionary{}
	if err = randomize.Struct(seed, dictionaryOne, dictionaryDBTypes, false, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}
	if err = randomize.Struct(seed, dictionaryTwo, dictionaryDBTypes, false, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = dictionaryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = dictionaryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testDictionariesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDictionariesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(dictionaryColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDictionariesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDictionariesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DictionarySlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDictionariesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Dictionaries().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	dictionaryDBTypes = map[string]string{`UUID`: `character`, `Title`: `text`, `Content`: `text`, `VersionNo`: `integer`, `ID`: `integer`}
	_                 = bytes.MinRead
)

func testDictionariesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(dictionaryPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(dictionaryAllColumns) == len(dictionaryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDictionariesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(dictionaryAllColumns) == len(dictionaryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Dictionary{}
	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, dictionaryDBTypes, true, dictionaryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(dictionaryAllColumns, dictionaryPrimaryKeyColumns) {
		fields = dictionaryAllColumns
	} else {
		fields = strmangle.SetComplement(
			dictionaryAllColumns,
			dictionaryPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DictionarySlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDictionariesUpsert(t *testing.T) {
	t.Parallel()

	if len(dictionaryAllColumns) == len(dictionaryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Dictionary{}
	if err = randomize.Struct(seed, &o, dictionaryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Dictionary: %s", err)
	}

	count, err := Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, dictionaryDBTypes, false, dictionaryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Dictionary struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Dictionary: %s", err)
	}

	count, err = Dictionaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
