// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// JudgementPage is an object representing the database table.
type JudgementPage struct {
	ID         string      `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	SuitNumber null.String `boil:"SuitNumber" json:"SuitNumber,omitempty" toml:"SuitNumber" yaml:"SuitNumber,omitempty"`
	Content    null.String `boil:"Content" json:"Content,omitempty" toml:"Content" yaml:"Content,omitempty"`
	Number     int         `boil:"Number" json:"Number" toml:"Number" yaml:"Number"`

	R *judgementPageR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L judgementPageL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var JudgementPageColumns = struct {
	ID         string
	SuitNumber string
	Content    string
	Number     string
}{
	ID:         "Id",
	SuitNumber: "SuitNumber",
	Content:    "Content",
	Number:     "Number",
}

// Generated where

var JudgementPageWhere = struct {
	ID         whereHelperstring
	SuitNumber whereHelpernull_String
	Content    whereHelpernull_String
	Number     whereHelperint
}{
	ID:         whereHelperstring{field: "\"JudgementPages\".\"Id\""},
	SuitNumber: whereHelpernull_String{field: "\"JudgementPages\".\"SuitNumber\""},
	Content:    whereHelpernull_String{field: "\"JudgementPages\".\"Content\""},
	Number:     whereHelperint{field: "\"JudgementPages\".\"Number\""},
}

// JudgementPageRels is where relationship names are stored.
var JudgementPageRels = struct {
}{}

// judgementPageR is where relationships are stored.
type judgementPageR struct {
}

// NewStruct creates a new relationship struct
func (*judgementPageR) NewStruct() *judgementPageR {
	return &judgementPageR{}
}

// judgementPageL is where Load methods for each relationship are stored.
type judgementPageL struct{}

var (
	judgementPageAllColumns            = []string{"Id", "SuitNumber", "Content", "Number"}
	judgementPageColumnsWithoutDefault = []string{"Id", "SuitNumber", "Content", "Number"}
	judgementPageColumnsWithDefault    = []string{}
	judgementPagePrimaryKeyColumns     = []string{"Id"}
)

type (
	// JudgementPageSlice is an alias for a slice of pointers to JudgementPage.
	// This should generally be used opposed to []JudgementPage.
	JudgementPageSlice []*JudgementPage

	judgementPageQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	judgementPageType                 = reflect.TypeOf(&JudgementPage{})
	judgementPageMapping              = queries.MakeStructMapping(judgementPageType)
	judgementPagePrimaryKeyMapping, _ = queries.BindMapping(judgementPageType, judgementPageMapping, judgementPagePrimaryKeyColumns)
	judgementPageInsertCacheMut       sync.RWMutex
	judgementPageInsertCache          = make(map[string]insertCache)
	judgementPageUpdateCacheMut       sync.RWMutex
	judgementPageUpdateCache          = make(map[string]updateCache)
	judgementPageUpsertCacheMut       sync.RWMutex
	judgementPageUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single judgementPage record from the query.
func (q judgementPageQuery) One(ctx context.Context, exec boil.ContextExecutor) (*JudgementPage, error) {
	o := &JudgementPage{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for JudgementPages")
	}

	return o, nil
}

// All returns all JudgementPage records from the query.
func (q judgementPageQuery) All(ctx context.Context, exec boil.ContextExecutor) (JudgementPageSlice, error) {
	var o []*JudgementPage

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to JudgementPage slice")
	}

	return o, nil
}

// Count returns the count of all JudgementPage records in the query.
func (q judgementPageQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count JudgementPages rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q judgementPageQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if JudgementPages exists")
	}

	return count > 0, nil
}

// JudgementPages retrieves all the records using an executor.
func JudgementPages(mods ...qm.QueryMod) judgementPageQuery {
	mods = append(mods, qm.From("\"JudgementPages\""))
	return judgementPageQuery{NewQuery(mods...)}
}

// FindJudgementPage retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindJudgementPage(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*JudgementPage, error) {
	judgementPageObj := &JudgementPage{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"JudgementPages\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, judgementPageObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from JudgementPages")
	}

	return judgementPageObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *JudgementPage) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no JudgementPages provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(judgementPageColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	judgementPageInsertCacheMut.RLock()
	cache, cached := judgementPageInsertCache[key]
	judgementPageInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			judgementPageAllColumns,
			judgementPageColumnsWithDefault,
			judgementPageColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(judgementPageType, judgementPageMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(judgementPageType, judgementPageMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"JudgementPages\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"JudgementPages\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into JudgementPages")
	}

	if !cached {
		judgementPageInsertCacheMut.Lock()
		judgementPageInsertCache[key] = cache
		judgementPageInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the JudgementPage.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *JudgementPage) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	judgementPageUpdateCacheMut.RLock()
	cache, cached := judgementPageUpdateCache[key]
	judgementPageUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			judgementPageAllColumns,
			judgementPagePrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update JudgementPages, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"JudgementPages\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, judgementPagePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(judgementPageType, judgementPageMapping, append(wl, judgementPagePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update JudgementPages row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for JudgementPages")
	}

	if !cached {
		judgementPageUpdateCacheMut.Lock()
		judgementPageUpdateCache[key] = cache
		judgementPageUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q judgementPageQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for JudgementPages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for JudgementPages")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o JudgementPageSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), judgementPagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"JudgementPages\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, judgementPagePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in judgementPage slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all judgementPage")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *JudgementPage) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no JudgementPages provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(judgementPageColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	judgementPageUpsertCacheMut.RLock()
	cache, cached := judgementPageUpsertCache[key]
	judgementPageUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			judgementPageAllColumns,
			judgementPageColumnsWithDefault,
			judgementPageColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			judgementPageAllColumns,
			judgementPagePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert JudgementPages, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(judgementPagePrimaryKeyColumns))
			copy(conflict, judgementPagePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"JudgementPages\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(judgementPageType, judgementPageMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(judgementPageType, judgementPageMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert JudgementPages")
	}

	if !cached {
		judgementPageUpsertCacheMut.Lock()
		judgementPageUpsertCache[key] = cache
		judgementPageUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single JudgementPage record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *JudgementPage) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no JudgementPage provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), judgementPagePrimaryKeyMapping)
	sql := "DELETE FROM \"JudgementPages\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from JudgementPages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for JudgementPages")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q judgementPageQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no judgementPageQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from JudgementPages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for JudgementPages")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o JudgementPageSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), judgementPagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"JudgementPages\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, judgementPagePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from judgementPage slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for JudgementPages")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *JudgementPage) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindJudgementPage(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *JudgementPageSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := JudgementPageSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), judgementPagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"JudgementPages\".* FROM \"JudgementPages\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, judgementPagePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in JudgementPageSlice")
	}

	*o = slice

	return nil
}

// JudgementPageExists checks if the JudgementPage row exists.
func JudgementPageExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"JudgementPages\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if JudgementPages exists")
	}

	return exists, nil
}
