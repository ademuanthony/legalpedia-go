// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SbjMatterIndex is an object representing the database table.
type SbjMatterIndex struct {
	ID                 int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	SubjectMatterIndex null.String `boil:"SubjectMatterIndex" json:"SubjectMatterIndex,omitempty" toml:"SubjectMatterIndex" yaml:"SubjectMatterIndex,omitempty"`
	UpdatedAt          null.Time   `boil:"UpdatedAt" json:"UpdatedAt,omitempty" toml:"UpdatedAt" yaml:"UpdatedAt,omitempty"`

	R *sbjMatterIndexR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L sbjMatterIndexL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SbjMatterIndexColumns = struct {
	ID                 string
	SubjectMatterIndex string
	UpdatedAt          string
}{
	ID:                 "Id",
	SubjectMatterIndex: "SubjectMatterIndex",
	UpdatedAt:          "UpdatedAt",
}

// Generated where

var SbjMatterIndexWhere = struct {
	ID                 whereHelperint
	SubjectMatterIndex whereHelpernull_String
	UpdatedAt          whereHelpernull_Time
}{
	ID:                 whereHelperint{field: "\"SbjMatterIndex\".\"Id\""},
	SubjectMatterIndex: whereHelpernull_String{field: "\"SbjMatterIndex\".\"SubjectMatterIndex\""},
	UpdatedAt:          whereHelpernull_Time{field: "\"SbjMatterIndex\".\"UpdatedAt\""},
}

// SbjMatterIndexRels is where relationship names are stored.
var SbjMatterIndexRels = struct {
}{}

// sbjMatterIndexR is where relationships are stored.
type sbjMatterIndexR struct {
}

// NewStruct creates a new relationship struct
func (*sbjMatterIndexR) NewStruct() *sbjMatterIndexR {
	return &sbjMatterIndexR{}
}

// sbjMatterIndexL is where Load methods for each relationship are stored.
type sbjMatterIndexL struct{}

var (
	sbjMatterIndexAllColumns            = []string{"Id", "SubjectMatterIndex", "UpdatedAt"}
	sbjMatterIndexColumnsWithoutDefault = []string{"Id", "SubjectMatterIndex", "UpdatedAt"}
	sbjMatterIndexColumnsWithDefault    = []string{}
	sbjMatterIndexPrimaryKeyColumns     = []string{"Id"}
)

type (
	// SbjMatterIndexSlice is an alias for a slice of pointers to SbjMatterIndex.
	// This should generally be used opposed to []SbjMatterIndex.
	SbjMatterIndexSlice []*SbjMatterIndex

	sbjMatterIndexQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	sbjMatterIndexType                 = reflect.TypeOf(&SbjMatterIndex{})
	sbjMatterIndexMapping              = queries.MakeStructMapping(sbjMatterIndexType)
	sbjMatterIndexPrimaryKeyMapping, _ = queries.BindMapping(sbjMatterIndexType, sbjMatterIndexMapping, sbjMatterIndexPrimaryKeyColumns)
	sbjMatterIndexInsertCacheMut       sync.RWMutex
	sbjMatterIndexInsertCache          = make(map[string]insertCache)
	sbjMatterIndexUpdateCacheMut       sync.RWMutex
	sbjMatterIndexUpdateCache          = make(map[string]updateCache)
	sbjMatterIndexUpsertCacheMut       sync.RWMutex
	sbjMatterIndexUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single sbjMatterIndex record from the query.
func (q sbjMatterIndexQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SbjMatterIndex, error) {
	o := &SbjMatterIndex{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for SbjMatterIndex")
	}

	return o, nil
}

// All returns all SbjMatterIndex records from the query.
func (q sbjMatterIndexQuery) All(ctx context.Context, exec boil.ContextExecutor) (SbjMatterIndexSlice, error) {
	var o []*SbjMatterIndex

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SbjMatterIndex slice")
	}

	return o, nil
}

// Count returns the count of all SbjMatterIndex records in the query.
func (q sbjMatterIndexQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count SbjMatterIndex rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q sbjMatterIndexQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if SbjMatterIndex exists")
	}

	return count > 0, nil
}

// SbjMatterIndexes retrieves all the records using an executor.
func SbjMatterIndexes(mods ...qm.QueryMod) sbjMatterIndexQuery {
	mods = append(mods, qm.From("\"SbjMatterIndex\""))
	return sbjMatterIndexQuery{NewQuery(mods...)}
}

// FindSbjMatterIndex retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSbjMatterIndex(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*SbjMatterIndex, error) {
	sbjMatterIndexObj := &SbjMatterIndex{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"SbjMatterIndex\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, sbjMatterIndexObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from SbjMatterIndex")
	}

	return sbjMatterIndexObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SbjMatterIndex) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SbjMatterIndex provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(sbjMatterIndexColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	sbjMatterIndexInsertCacheMut.RLock()
	cache, cached := sbjMatterIndexInsertCache[key]
	sbjMatterIndexInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			sbjMatterIndexAllColumns,
			sbjMatterIndexColumnsWithDefault,
			sbjMatterIndexColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(sbjMatterIndexType, sbjMatterIndexMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(sbjMatterIndexType, sbjMatterIndexMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"SbjMatterIndex\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"SbjMatterIndex\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into SbjMatterIndex")
	}

	if !cached {
		sbjMatterIndexInsertCacheMut.Lock()
		sbjMatterIndexInsertCache[key] = cache
		sbjMatterIndexInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the SbjMatterIndex.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SbjMatterIndex) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	sbjMatterIndexUpdateCacheMut.RLock()
	cache, cached := sbjMatterIndexUpdateCache[key]
	sbjMatterIndexUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			sbjMatterIndexAllColumns,
			sbjMatterIndexPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update SbjMatterIndex, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"SbjMatterIndex\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, sbjMatterIndexPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(sbjMatterIndexType, sbjMatterIndexMapping, append(wl, sbjMatterIndexPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update SbjMatterIndex row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for SbjMatterIndex")
	}

	if !cached {
		sbjMatterIndexUpdateCacheMut.Lock()
		sbjMatterIndexUpdateCache[key] = cache
		sbjMatterIndexUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q sbjMatterIndexQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for SbjMatterIndex")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for SbjMatterIndex")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SbjMatterIndexSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sbjMatterIndexPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"SbjMatterIndex\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, sbjMatterIndexPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in sbjMatterIndex slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all sbjMatterIndex")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SbjMatterIndex) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SbjMatterIndex provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(sbjMatterIndexColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	sbjMatterIndexUpsertCacheMut.RLock()
	cache, cached := sbjMatterIndexUpsertCache[key]
	sbjMatterIndexUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			sbjMatterIndexAllColumns,
			sbjMatterIndexColumnsWithDefault,
			sbjMatterIndexColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			sbjMatterIndexAllColumns,
			sbjMatterIndexPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert SbjMatterIndex, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(sbjMatterIndexPrimaryKeyColumns))
			copy(conflict, sbjMatterIndexPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"SbjMatterIndex\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(sbjMatterIndexType, sbjMatterIndexMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(sbjMatterIndexType, sbjMatterIndexMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert SbjMatterIndex")
	}

	if !cached {
		sbjMatterIndexUpsertCacheMut.Lock()
		sbjMatterIndexUpsertCache[key] = cache
		sbjMatterIndexUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single SbjMatterIndex record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SbjMatterIndex) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SbjMatterIndex provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), sbjMatterIndexPrimaryKeyMapping)
	sql := "DELETE FROM \"SbjMatterIndex\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from SbjMatterIndex")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for SbjMatterIndex")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q sbjMatterIndexQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no sbjMatterIndexQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from SbjMatterIndex")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SbjMatterIndex")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SbjMatterIndexSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sbjMatterIndexPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"SbjMatterIndex\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sbjMatterIndexPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from sbjMatterIndex slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SbjMatterIndex")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SbjMatterIndex) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSbjMatterIndex(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SbjMatterIndexSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SbjMatterIndexSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sbjMatterIndexPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"SbjMatterIndex\".* FROM \"SbjMatterIndex\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sbjMatterIndexPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SbjMatterIndexSlice")
	}

	*o = slice

	return nil
}

// SbjMatterIndexExists checks if the SbjMatterIndex row exists.
func SbjMatterIndexExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"SbjMatterIndex\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if SbjMatterIndex exists")
	}

	return exists, nil
}
