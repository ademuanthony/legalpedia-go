// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSharedNotes(t *testing.T) {
	t.Parallel()

	query := SharedNotes()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSharedNotesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSharedNotesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SharedNotes().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSharedNotesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SharedNoteSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSharedNotesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SharedNoteExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SharedNote exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SharedNoteExists to return true, but got false.")
	}
}

func testSharedNotesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	sharedNoteFound, err := FindSharedNote(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if sharedNoteFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSharedNotesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SharedNotes().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testSharedNotesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SharedNotes().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSharedNotesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	sharedNoteOne := &SharedNote{}
	sharedNoteTwo := &SharedNote{}
	if err = randomize.Struct(seed, sharedNoteOne, sharedNoteDBTypes, false, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}
	if err = randomize.Struct(seed, sharedNoteTwo, sharedNoteDBTypes, false, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = sharedNoteOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = sharedNoteTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SharedNotes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSharedNotesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	sharedNoteOne := &SharedNote{}
	sharedNoteTwo := &SharedNote{}
	if err = randomize.Struct(seed, sharedNoteOne, sharedNoteDBTypes, false, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}
	if err = randomize.Struct(seed, sharedNoteTwo, sharedNoteDBTypes, false, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = sharedNoteOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = sharedNoteTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSharedNotesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSharedNotesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(sharedNoteColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSharedNotesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSharedNotesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SharedNoteSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSharedNotesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SharedNotes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	sharedNoteDBTypes = map[string]string{`ID`: `text`, `NoteId`: `text`, `CreatedAt`: `timestamp without time zone`, `UserId`: `bigint`, `TargetUserId`: `bigint`}
	_                 = bytes.MinRead
)

func testSharedNotesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(sharedNotePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(sharedNoteAllColumns) == len(sharedNotePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNotePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSharedNotesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(sharedNoteAllColumns) == len(sharedNotePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SharedNote{}
	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, sharedNoteDBTypes, true, sharedNotePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(sharedNoteAllColumns, sharedNotePrimaryKeyColumns) {
		fields = sharedNoteAllColumns
	} else {
		fields = strmangle.SetComplement(
			sharedNoteAllColumns,
			sharedNotePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SharedNoteSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSharedNotesUpsert(t *testing.T) {
	t.Parallel()

	if len(sharedNoteAllColumns) == len(sharedNotePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SharedNote{}
	if err = randomize.Struct(seed, &o, sharedNoteDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SharedNote: %s", err)
	}

	count, err := SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, sharedNoteDBTypes, false, sharedNotePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SharedNote struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SharedNote: %s", err)
	}

	count, err = SharedNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
