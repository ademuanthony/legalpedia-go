// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testLicenses(t *testing.T) {
	t.Parallel()

	query := Licenses()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testLicensesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testLicensesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Licenses().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testLicensesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := LicenseSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testLicensesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := LicenseExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if License exists: %s", err)
	}
	if !e {
		t.Errorf("Expected LicenseExists to return true, but got false.")
	}
}

func testLicensesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	licenseFound, err := FindLicense(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if licenseFound == nil {
		t.Error("want a record, got nil")
	}
}

func testLicensesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Licenses().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testLicensesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Licenses().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testLicensesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	licenseOne := &License{}
	licenseTwo := &License{}
	if err = randomize.Struct(seed, licenseOne, licenseDBTypes, false, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}
	if err = randomize.Struct(seed, licenseTwo, licenseDBTypes, false, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = licenseOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = licenseTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Licenses().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testLicensesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	licenseOne := &License{}
	licenseTwo := &License{}
	if err = randomize.Struct(seed, licenseOne, licenseDBTypes, false, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}
	if err = randomize.Struct(seed, licenseTwo, licenseDBTypes, false, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = licenseOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = licenseTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testLicensesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testLicensesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(licenseColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testLicensesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testLicensesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := LicenseSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testLicensesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Licenses().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	licenseDBTypes = map[string]string{`ID`: `integer`, `CustomerId`: `integer`, `SystemId`: `text`, `LicensedDays`: `integer`, `UnlockDate`: `timestamp without time zone`, `Package`: `text`, `LastModificationTime`: `timestamp without time zone`, `LastModifierUserId`: `bigint`, `CreationTime`: `timestamp without time zone`, `CreatorUserId`: `bigint`, `TenantId`: `integer`, `UpdateLicensedDays`: `integer`, `MobileSystemId`: `text`, `ExpDate`: `timestamp without time zone`}
	_              = bytes.MinRead
)

func testLicensesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(licensePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(licenseAllColumns) == len(licensePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, licenseDBTypes, true, licensePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testLicensesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(licenseAllColumns) == len(licensePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &License{}
	if err = randomize.Struct(seed, o, licenseDBTypes, true, licenseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, licenseDBTypes, true, licensePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(licenseAllColumns, licensePrimaryKeyColumns) {
		fields = licenseAllColumns
	} else {
		fields = strmangle.SetComplement(
			licenseAllColumns,
			licensePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := LicenseSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testLicensesUpsert(t *testing.T) {
	t.Parallel()

	if len(licenseAllColumns) == len(licensePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := License{}
	if err = randomize.Struct(seed, &o, licenseDBTypes, true); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert License: %s", err)
	}

	count, err := Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, licenseDBTypes, false, licensePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize License struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert License: %s", err)
	}

	count, err = Licenses().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
