// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testTeamLogos(t *testing.T) {
	t.Parallel()

	query := TeamLogos()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testTeamLogosDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTeamLogosQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := TeamLogos().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTeamLogosSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TeamLogoSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTeamLogosExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := TeamLogoExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if TeamLogo exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TeamLogoExists to return true, but got false.")
	}
}

func testTeamLogosFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	teamLogoFound, err := FindTeamLogo(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if teamLogoFound == nil {
		t.Error("want a record, got nil")
	}
}

func testTeamLogosBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = TeamLogos().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testTeamLogosOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := TeamLogos().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTeamLogosAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	teamLogoOne := &TeamLogo{}
	teamLogoTwo := &TeamLogo{}
	if err = randomize.Struct(seed, teamLogoOne, teamLogoDBTypes, false, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}
	if err = randomize.Struct(seed, teamLogoTwo, teamLogoDBTypes, false, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = teamLogoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = teamLogoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TeamLogos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTeamLogosCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	teamLogoOne := &TeamLogo{}
	teamLogoTwo := &TeamLogo{}
	if err = randomize.Struct(seed, teamLogoOne, teamLogoDBTypes, false, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}
	if err = randomize.Struct(seed, teamLogoTwo, teamLogoDBTypes, false, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = teamLogoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = teamLogoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testTeamLogosInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTeamLogosInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(teamLogoColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTeamLogosReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTeamLogosReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TeamLogoSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTeamLogosSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TeamLogos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	teamLogoDBTypes = map[string]string{`ID`: `text`, `TeamId`: `text`, `Base64`: `text`}
	_               = bytes.MinRead
)

func testTeamLogosUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(teamLogoPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(teamLogoAllColumns) == len(teamLogoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testTeamLogosSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(teamLogoAllColumns) == len(teamLogoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TeamLogo{}
	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, teamLogoDBTypes, true, teamLogoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(teamLogoAllColumns, teamLogoPrimaryKeyColumns) {
		fields = teamLogoAllColumns
	} else {
		fields = strmangle.SetComplement(
			teamLogoAllColumns,
			teamLogoPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := TeamLogoSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testTeamLogosUpsert(t *testing.T) {
	t.Parallel()

	if len(teamLogoAllColumns) == len(teamLogoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := TeamLogo{}
	if err = randomize.Struct(seed, &o, teamLogoDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TeamLogo: %s", err)
	}

	count, err := TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, teamLogoDBTypes, false, teamLogoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TeamLogo struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TeamLogo: %s", err)
	}

	count, err = TeamLogos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
