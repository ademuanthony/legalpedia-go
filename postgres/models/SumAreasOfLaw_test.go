// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSumAreasOfLaws(t *testing.T) {
	t.Parallel()

	query := SumAreasOfLaws()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSumAreasOfLawsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSumAreasOfLawsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SumAreasOfLaws().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSumAreasOfLawsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SumAreasOfLawSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSumAreasOfLawsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SumAreasOfLawExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SumAreasOfLaw exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SumAreasOfLawExists to return true, but got false.")
	}
}

func testSumAreasOfLawsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	sumAreasOfLawFound, err := FindSumAreasOfLaw(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if sumAreasOfLawFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSumAreasOfLawsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SumAreasOfLaws().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testSumAreasOfLawsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SumAreasOfLaws().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSumAreasOfLawsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	sumAreasOfLawOne := &SumAreasOfLaw{}
	sumAreasOfLawTwo := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, sumAreasOfLawOne, sumAreasOfLawDBTypes, false, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}
	if err = randomize.Struct(seed, sumAreasOfLawTwo, sumAreasOfLawDBTypes, false, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = sumAreasOfLawOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = sumAreasOfLawTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SumAreasOfLaws().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSumAreasOfLawsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	sumAreasOfLawOne := &SumAreasOfLaw{}
	sumAreasOfLawTwo := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, sumAreasOfLawOne, sumAreasOfLawDBTypes, false, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}
	if err = randomize.Struct(seed, sumAreasOfLawTwo, sumAreasOfLawDBTypes, false, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = sumAreasOfLawOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = sumAreasOfLawTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSumAreasOfLawsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSumAreasOfLawsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(sumAreasOfLawColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSumAreasOfLawsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSumAreasOfLawsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SumAreasOfLawSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSumAreasOfLawsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SumAreasOfLaws().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	sumAreasOfLawDBTypes = map[string]string{`ID`: `integer`, `SuitNo`: `character varying`, `AreaOfLawID`: `integer`}
	_                    = bytes.MinRead
)

func testSumAreasOfLawsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(sumAreasOfLawPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(sumAreasOfLawAllColumns) == len(sumAreasOfLawPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSumAreasOfLawsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(sumAreasOfLawAllColumns) == len(sumAreasOfLawPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SumAreasOfLaw{}
	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, sumAreasOfLawDBTypes, true, sumAreasOfLawPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(sumAreasOfLawAllColumns, sumAreasOfLawPrimaryKeyColumns) {
		fields = sumAreasOfLawAllColumns
	} else {
		fields = strmangle.SetComplement(
			sumAreasOfLawAllColumns,
			sumAreasOfLawPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SumAreasOfLawSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSumAreasOfLawsUpsert(t *testing.T) {
	t.Parallel()

	if len(sumAreasOfLawAllColumns) == len(sumAreasOfLawPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SumAreasOfLaw{}
	if err = randomize.Struct(seed, &o, sumAreasOfLawDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SumAreasOfLaw: %s", err)
	}

	count, err := SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, sumAreasOfLawDBTypes, false, sumAreasOfLawPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SumAreasOfLaw struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SumAreasOfLaw: %s", err)
	}

	count, err = SumAreasOfLaws().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
