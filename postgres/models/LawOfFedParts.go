// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// LawOfFedPart is an object representing the database table.
type LawOfFedPart struct {
	ID         int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	PartHeader null.String `boil:"PartHeader" json:"PartHeader,omitempty" toml:"PartHeader" yaml:"PartHeader,omitempty"`
	LawId      null.Int    `boil:"LawId" json:"LawId,omitempty" toml:"LawId" yaml:"LawId,omitempty"`

	R *lawOfFedPartR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lawOfFedPartL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LawOfFedPartColumns = struct {
	ID         string
	PartHeader string
	LawId      string
}{
	ID:         "Id",
	PartHeader: "PartHeader",
	LawId:      "LawId",
}

// Generated where

var LawOfFedPartWhere = struct {
	ID         whereHelperint
	PartHeader whereHelpernull_String
	LawId      whereHelpernull_Int
}{
	ID:         whereHelperint{field: "\"LawOfFedParts\".\"Id\""},
	PartHeader: whereHelpernull_String{field: "\"LawOfFedParts\".\"PartHeader\""},
	LawId:      whereHelpernull_Int{field: "\"LawOfFedParts\".\"LawId\""},
}

// LawOfFedPartRels is where relationship names are stored.
var LawOfFedPartRels = struct {
}{}

// lawOfFedPartR is where relationships are stored.
type lawOfFedPartR struct {
}

// NewStruct creates a new relationship struct
func (*lawOfFedPartR) NewStruct() *lawOfFedPartR {
	return &lawOfFedPartR{}
}

// lawOfFedPartL is where Load methods for each relationship are stored.
type lawOfFedPartL struct{}

var (
	lawOfFedPartAllColumns            = []string{"Id", "PartHeader", "LawId"}
	lawOfFedPartColumnsWithoutDefault = []string{"Id", "PartHeader", "LawId"}
	lawOfFedPartColumnsWithDefault    = []string{}
	lawOfFedPartPrimaryKeyColumns     = []string{"Id"}
)

type (
	// LawOfFedPartSlice is an alias for a slice of pointers to LawOfFedPart.
	// This should generally be used opposed to []LawOfFedPart.
	LawOfFedPartSlice []*LawOfFedPart

	lawOfFedPartQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lawOfFedPartType                 = reflect.TypeOf(&LawOfFedPart{})
	lawOfFedPartMapping              = queries.MakeStructMapping(lawOfFedPartType)
	lawOfFedPartPrimaryKeyMapping, _ = queries.BindMapping(lawOfFedPartType, lawOfFedPartMapping, lawOfFedPartPrimaryKeyColumns)
	lawOfFedPartInsertCacheMut       sync.RWMutex
	lawOfFedPartInsertCache          = make(map[string]insertCache)
	lawOfFedPartUpdateCacheMut       sync.RWMutex
	lawOfFedPartUpdateCache          = make(map[string]updateCache)
	lawOfFedPartUpsertCacheMut       sync.RWMutex
	lawOfFedPartUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single lawOfFedPart record from the query.
func (q lawOfFedPartQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LawOfFedPart, error) {
	o := &LawOfFedPart{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for LawOfFedParts")
	}

	return o, nil
}

// All returns all LawOfFedPart records from the query.
func (q lawOfFedPartQuery) All(ctx context.Context, exec boil.ContextExecutor) (LawOfFedPartSlice, error) {
	var o []*LawOfFedPart

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to LawOfFedPart slice")
	}

	return o, nil
}

// Count returns the count of all LawOfFedPart records in the query.
func (q lawOfFedPartQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count LawOfFedParts rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lawOfFedPartQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if LawOfFedParts exists")
	}

	return count > 0, nil
}

// LawOfFedParts retrieves all the records using an executor.
func LawOfFedParts(mods ...qm.QueryMod) lawOfFedPartQuery {
	mods = append(mods, qm.From("\"LawOfFedParts\""))
	return lawOfFedPartQuery{NewQuery(mods...)}
}

// FindLawOfFedPart retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLawOfFedPart(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*LawOfFedPart, error) {
	lawOfFedPartObj := &LawOfFedPart{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"LawOfFedParts\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lawOfFedPartObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from LawOfFedParts")
	}

	return lawOfFedPartObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LawOfFedPart) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no LawOfFedParts provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(lawOfFedPartColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lawOfFedPartInsertCacheMut.RLock()
	cache, cached := lawOfFedPartInsertCache[key]
	lawOfFedPartInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lawOfFedPartAllColumns,
			lawOfFedPartColumnsWithDefault,
			lawOfFedPartColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lawOfFedPartType, lawOfFedPartMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lawOfFedPartType, lawOfFedPartMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"LawOfFedParts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"LawOfFedParts\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into LawOfFedParts")
	}

	if !cached {
		lawOfFedPartInsertCacheMut.Lock()
		lawOfFedPartInsertCache[key] = cache
		lawOfFedPartInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the LawOfFedPart.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LawOfFedPart) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	lawOfFedPartUpdateCacheMut.RLock()
	cache, cached := lawOfFedPartUpdateCache[key]
	lawOfFedPartUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lawOfFedPartAllColumns,
			lawOfFedPartPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update LawOfFedParts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"LawOfFedParts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, lawOfFedPartPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lawOfFedPartType, lawOfFedPartMapping, append(wl, lawOfFedPartPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update LawOfFedParts row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for LawOfFedParts")
	}

	if !cached {
		lawOfFedPartUpdateCacheMut.Lock()
		lawOfFedPartUpdateCache[key] = cache
		lawOfFedPartUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q lawOfFedPartQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for LawOfFedParts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for LawOfFedParts")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LawOfFedPartSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawOfFedPartPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"LawOfFedParts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, lawOfFedPartPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in lawOfFedPart slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all lawOfFedPart")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LawOfFedPart) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no LawOfFedParts provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(lawOfFedPartColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lawOfFedPartUpsertCacheMut.RLock()
	cache, cached := lawOfFedPartUpsertCache[key]
	lawOfFedPartUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			lawOfFedPartAllColumns,
			lawOfFedPartColumnsWithDefault,
			lawOfFedPartColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			lawOfFedPartAllColumns,
			lawOfFedPartPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert LawOfFedParts, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(lawOfFedPartPrimaryKeyColumns))
			copy(conflict, lawOfFedPartPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"LawOfFedParts\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(lawOfFedPartType, lawOfFedPartMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lawOfFedPartType, lawOfFedPartMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert LawOfFedParts")
	}

	if !cached {
		lawOfFedPartUpsertCacheMut.Lock()
		lawOfFedPartUpsertCache[key] = cache
		lawOfFedPartUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single LawOfFedPart record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LawOfFedPart) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no LawOfFedPart provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lawOfFedPartPrimaryKeyMapping)
	sql := "DELETE FROM \"LawOfFedParts\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from LawOfFedParts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for LawOfFedParts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lawOfFedPartQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no lawOfFedPartQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from LawOfFedParts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for LawOfFedParts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LawOfFedPartSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawOfFedPartPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"LawOfFedParts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lawOfFedPartPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lawOfFedPart slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for LawOfFedParts")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LawOfFedPart) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLawOfFedPart(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LawOfFedPartSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LawOfFedPartSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawOfFedPartPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"LawOfFedParts\".* FROM \"LawOfFedParts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lawOfFedPartPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in LawOfFedPartSlice")
	}

	*o = slice

	return nil
}

// LawOfFedPartExists checks if the LawOfFedPart row exists.
func LawOfFedPartExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"LawOfFedParts\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if LawOfFedParts exists")
	}

	return exists, nil
}
