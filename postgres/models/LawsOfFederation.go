// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// LawsOfFederation is an object representing the database table.
type LawsOfFederation struct {
	ID                    int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	CatId                 null.Int    `boil:"CatId" json:"CatId,omitempty" toml:"CatId" yaml:"CatId,omitempty"`
	LawNo                 null.String `boil:"LawNo" json:"LawNo,omitempty" toml:"LawNo" yaml:"LawNo,omitempty"`
	Title                 null.String `boil:"Title" json:"Title,omitempty" toml:"Title" yaml:"Title,omitempty"`
	LawDate               null.Time   `boil:"LawDate" json:"LawDate,omitempty" toml:"LawDate" yaml:"LawDate,omitempty"`
	Descr                 null.String `boil:"Descr" json:"Descr,omitempty" toml:"Descr" yaml:"Descr,omitempty"`
	SubsidiaryLegislation null.String `boil:"SubsidiaryLegislation" json:"SubsidiaryLegislation,omitempty" toml:"SubsidiaryLegislation" yaml:"SubsidiaryLegislation,omitempty"`
	Tags                  null.String `boil:"Tags" json:"Tags,omitempty" toml:"Tags" yaml:"Tags,omitempty"`
	CreatedAt             null.Time   `boil:"CreatedAt" json:"CreatedAt,omitempty" toml:"CreatedAt" yaml:"CreatedAt,omitempty"`
	UpdatedAt             null.Time   `boil:"UpdatedAt" json:"UpdatedAt,omitempty" toml:"UpdatedAt" yaml:"UpdatedAt,omitempty"`

	R *lawsOfFederationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lawsOfFederationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LawsOfFederationColumns = struct {
	ID                    string
	CatId                 string
	LawNo                 string
	Title                 string
	LawDate               string
	Descr                 string
	SubsidiaryLegislation string
	Tags                  string
	CreatedAt             string
	UpdatedAt             string
}{
	ID:                    "Id",
	CatId:                 "CatId",
	LawNo:                 "LawNo",
	Title:                 "Title",
	LawDate:               "LawDate",
	Descr:                 "Descr",
	SubsidiaryLegislation: "SubsidiaryLegislation",
	Tags:                  "Tags",
	CreatedAt:             "CreatedAt",
	UpdatedAt:             "UpdatedAt",
}

// Generated where

var LawsOfFederationWhere = struct {
	ID                    whereHelperint
	CatId                 whereHelpernull_Int
	LawNo                 whereHelpernull_String
	Title                 whereHelpernull_String
	LawDate               whereHelpernull_Time
	Descr                 whereHelpernull_String
	SubsidiaryLegislation whereHelpernull_String
	Tags                  whereHelpernull_String
	CreatedAt             whereHelpernull_Time
	UpdatedAt             whereHelpernull_Time
}{
	ID:                    whereHelperint{field: "\"LawsOfFederation\".\"Id\""},
	CatId:                 whereHelpernull_Int{field: "\"LawsOfFederation\".\"CatId\""},
	LawNo:                 whereHelpernull_String{field: "\"LawsOfFederation\".\"LawNo\""},
	Title:                 whereHelpernull_String{field: "\"LawsOfFederation\".\"Title\""},
	LawDate:               whereHelpernull_Time{field: "\"LawsOfFederation\".\"LawDate\""},
	Descr:                 whereHelpernull_String{field: "\"LawsOfFederation\".\"Descr\""},
	SubsidiaryLegislation: whereHelpernull_String{field: "\"LawsOfFederation\".\"SubsidiaryLegislation\""},
	Tags:                  whereHelpernull_String{field: "\"LawsOfFederation\".\"Tags\""},
	CreatedAt:             whereHelpernull_Time{field: "\"LawsOfFederation\".\"CreatedAt\""},
	UpdatedAt:             whereHelpernull_Time{field: "\"LawsOfFederation\".\"UpdatedAt\""},
}

// LawsOfFederationRels is where relationship names are stored.
var LawsOfFederationRels = struct {
}{}

// lawsOfFederationR is where relationships are stored.
type lawsOfFederationR struct {
}

// NewStruct creates a new relationship struct
func (*lawsOfFederationR) NewStruct() *lawsOfFederationR {
	return &lawsOfFederationR{}
}

// lawsOfFederationL is where Load methods for each relationship are stored.
type lawsOfFederationL struct{}

var (
	lawsOfFederationAllColumns            = []string{"Id", "CatId", "LawNo", "Title", "LawDate", "Descr", "SubsidiaryLegislation", "Tags", "CreatedAt", "UpdatedAt"}
	lawsOfFederationColumnsWithoutDefault = []string{"Id", "CatId", "LawNo", "Title", "LawDate", "Descr", "SubsidiaryLegislation", "Tags", "CreatedAt", "UpdatedAt"}
	lawsOfFederationColumnsWithDefault    = []string{}
	lawsOfFederationPrimaryKeyColumns     = []string{"Id"}
)

type (
	// LawsOfFederationSlice is an alias for a slice of pointers to LawsOfFederation.
	// This should generally be used opposed to []LawsOfFederation.
	LawsOfFederationSlice []*LawsOfFederation

	lawsOfFederationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lawsOfFederationType                 = reflect.TypeOf(&LawsOfFederation{})
	lawsOfFederationMapping              = queries.MakeStructMapping(lawsOfFederationType)
	lawsOfFederationPrimaryKeyMapping, _ = queries.BindMapping(lawsOfFederationType, lawsOfFederationMapping, lawsOfFederationPrimaryKeyColumns)
	lawsOfFederationInsertCacheMut       sync.RWMutex
	lawsOfFederationInsertCache          = make(map[string]insertCache)
	lawsOfFederationUpdateCacheMut       sync.RWMutex
	lawsOfFederationUpdateCache          = make(map[string]updateCache)
	lawsOfFederationUpsertCacheMut       sync.RWMutex
	lawsOfFederationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single lawsOfFederation record from the query.
func (q lawsOfFederationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LawsOfFederation, error) {
	o := &LawsOfFederation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for LawsOfFederation")
	}

	return o, nil
}

// All returns all LawsOfFederation records from the query.
func (q lawsOfFederationQuery) All(ctx context.Context, exec boil.ContextExecutor) (LawsOfFederationSlice, error) {
	var o []*LawsOfFederation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to LawsOfFederation slice")
	}

	return o, nil
}

// Count returns the count of all LawsOfFederation records in the query.
func (q lawsOfFederationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count LawsOfFederation rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lawsOfFederationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if LawsOfFederation exists")
	}

	return count > 0, nil
}

// LawsOfFederations retrieves all the records using an executor.
func LawsOfFederations(mods ...qm.QueryMod) lawsOfFederationQuery {
	mods = append(mods, qm.From("\"LawsOfFederation\""))
	return lawsOfFederationQuery{NewQuery(mods...)}
}

// FindLawsOfFederation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLawsOfFederation(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*LawsOfFederation, error) {
	lawsOfFederationObj := &LawsOfFederation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"LawsOfFederation\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lawsOfFederationObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from LawsOfFederation")
	}

	return lawsOfFederationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LawsOfFederation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no LawsOfFederation provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(lawsOfFederationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lawsOfFederationInsertCacheMut.RLock()
	cache, cached := lawsOfFederationInsertCache[key]
	lawsOfFederationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lawsOfFederationAllColumns,
			lawsOfFederationColumnsWithDefault,
			lawsOfFederationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lawsOfFederationType, lawsOfFederationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lawsOfFederationType, lawsOfFederationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"LawsOfFederation\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"LawsOfFederation\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into LawsOfFederation")
	}

	if !cached {
		lawsOfFederationInsertCacheMut.Lock()
		lawsOfFederationInsertCache[key] = cache
		lawsOfFederationInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the LawsOfFederation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LawsOfFederation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	lawsOfFederationUpdateCacheMut.RLock()
	cache, cached := lawsOfFederationUpdateCache[key]
	lawsOfFederationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lawsOfFederationAllColumns,
			lawsOfFederationPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update LawsOfFederation, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"LawsOfFederation\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, lawsOfFederationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lawsOfFederationType, lawsOfFederationMapping, append(wl, lawsOfFederationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update LawsOfFederation row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for LawsOfFederation")
	}

	if !cached {
		lawsOfFederationUpdateCacheMut.Lock()
		lawsOfFederationUpdateCache[key] = cache
		lawsOfFederationUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q lawsOfFederationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for LawsOfFederation")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for LawsOfFederation")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LawsOfFederationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawsOfFederationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"LawsOfFederation\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, lawsOfFederationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in lawsOfFederation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all lawsOfFederation")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LawsOfFederation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no LawsOfFederation provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(lawsOfFederationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lawsOfFederationUpsertCacheMut.RLock()
	cache, cached := lawsOfFederationUpsertCache[key]
	lawsOfFederationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			lawsOfFederationAllColumns,
			lawsOfFederationColumnsWithDefault,
			lawsOfFederationColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			lawsOfFederationAllColumns,
			lawsOfFederationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert LawsOfFederation, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(lawsOfFederationPrimaryKeyColumns))
			copy(conflict, lawsOfFederationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"LawsOfFederation\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(lawsOfFederationType, lawsOfFederationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lawsOfFederationType, lawsOfFederationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert LawsOfFederation")
	}

	if !cached {
		lawsOfFederationUpsertCacheMut.Lock()
		lawsOfFederationUpsertCache[key] = cache
		lawsOfFederationUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single LawsOfFederation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LawsOfFederation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no LawsOfFederation provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lawsOfFederationPrimaryKeyMapping)
	sql := "DELETE FROM \"LawsOfFederation\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from LawsOfFederation")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for LawsOfFederation")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lawsOfFederationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no lawsOfFederationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from LawsOfFederation")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for LawsOfFederation")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LawsOfFederationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawsOfFederationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"LawsOfFederation\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lawsOfFederationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lawsOfFederation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for LawsOfFederation")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LawsOfFederation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLawsOfFederation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LawsOfFederationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LawsOfFederationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawsOfFederationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"LawsOfFederation\".* FROM \"LawsOfFederation\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lawsOfFederationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in LawsOfFederationSlice")
	}

	*o = slice

	return nil
}

// LawsOfFederationExists checks if the LawsOfFederation row exists.
func LawsOfFederationExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"LawsOfFederation\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if LawsOfFederation exists")
	}

	return exists, nil
}
