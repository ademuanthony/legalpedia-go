// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testOneTimePasswords(t *testing.T) {
	t.Parallel()

	query := OneTimePasswords()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testOneTimePasswordsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testOneTimePasswordsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := OneTimePasswords().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testOneTimePasswordsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := OneTimePasswordSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testOneTimePasswordsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := OneTimePasswordExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if OneTimePassword exists: %s", err)
	}
	if !e {
		t.Errorf("Expected OneTimePasswordExists to return true, but got false.")
	}
}

func testOneTimePasswordsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	oneTimePasswordFound, err := FindOneTimePassword(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if oneTimePasswordFound == nil {
		t.Error("want a record, got nil")
	}
}

func testOneTimePasswordsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = OneTimePasswords().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testOneTimePasswordsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := OneTimePasswords().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testOneTimePasswordsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	oneTimePasswordOne := &OneTimePassword{}
	oneTimePasswordTwo := &OneTimePassword{}
	if err = randomize.Struct(seed, oneTimePasswordOne, oneTimePasswordDBTypes, false, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}
	if err = randomize.Struct(seed, oneTimePasswordTwo, oneTimePasswordDBTypes, false, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = oneTimePasswordOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = oneTimePasswordTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := OneTimePasswords().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testOneTimePasswordsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	oneTimePasswordOne := &OneTimePassword{}
	oneTimePasswordTwo := &OneTimePassword{}
	if err = randomize.Struct(seed, oneTimePasswordOne, oneTimePasswordDBTypes, false, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}
	if err = randomize.Struct(seed, oneTimePasswordTwo, oneTimePasswordDBTypes, false, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = oneTimePasswordOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = oneTimePasswordTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testOneTimePasswordsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testOneTimePasswordsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(oneTimePasswordColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testOneTimePasswordsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testOneTimePasswordsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := OneTimePasswordSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testOneTimePasswordsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := OneTimePasswords().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	oneTimePasswordDBTypes = map[string]string{`ID`: `integer`, `Code`: `text`, `CustomerId`: `integer`, `SystemId`: `text`, `LicenseId`: `bigint`, `CreationTime`: `timestamp without time zone`, `TenantId`: `integer`}
	_                      = bytes.MinRead
)

func testOneTimePasswordsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(oneTimePasswordPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(oneTimePasswordAllColumns) == len(oneTimePasswordPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testOneTimePasswordsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(oneTimePasswordAllColumns) == len(oneTimePasswordPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &OneTimePassword{}
	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, oneTimePasswordDBTypes, true, oneTimePasswordPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(oneTimePasswordAllColumns, oneTimePasswordPrimaryKeyColumns) {
		fields = oneTimePasswordAllColumns
	} else {
		fields = strmangle.SetComplement(
			oneTimePasswordAllColumns,
			oneTimePasswordPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := OneTimePasswordSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testOneTimePasswordsUpsert(t *testing.T) {
	t.Parallel()

	if len(oneTimePasswordAllColumns) == len(oneTimePasswordPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := OneTimePassword{}
	if err = randomize.Struct(seed, &o, oneTimePasswordDBTypes, true); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert OneTimePassword: %s", err)
	}

	count, err := OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, oneTimePasswordDBTypes, false, oneTimePasswordPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize OneTimePassword struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert OneTimePassword: %s", err)
	}

	count, err = OneTimePasswords().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
