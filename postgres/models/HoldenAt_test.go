// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testHoldenAts(t *testing.T) {
	t.Parallel()

	query := HoldenAts()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testHoldenAtsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testHoldenAtsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := HoldenAts().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testHoldenAtsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := HoldenAtSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testHoldenAtsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := HoldenAtExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if HoldenAt exists: %s", err)
	}
	if !e {
		t.Errorf("Expected HoldenAtExists to return true, but got false.")
	}
}

func testHoldenAtsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	holdenAtFound, err := FindHoldenAt(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if holdenAtFound == nil {
		t.Error("want a record, got nil")
	}
}

func testHoldenAtsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = HoldenAts().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testHoldenAtsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := HoldenAts().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testHoldenAtsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	holdenAtOne := &HoldenAt{}
	holdenAtTwo := &HoldenAt{}
	if err = randomize.Struct(seed, holdenAtOne, holdenAtDBTypes, false, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}
	if err = randomize.Struct(seed, holdenAtTwo, holdenAtDBTypes, false, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = holdenAtOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = holdenAtTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := HoldenAts().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testHoldenAtsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	holdenAtOne := &HoldenAt{}
	holdenAtTwo := &HoldenAt{}
	if err = randomize.Struct(seed, holdenAtOne, holdenAtDBTypes, false, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}
	if err = randomize.Struct(seed, holdenAtTwo, holdenAtDBTypes, false, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = holdenAtOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = holdenAtTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testHoldenAtsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testHoldenAtsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(holdenAtColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testHoldenAtsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testHoldenAtsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := HoldenAtSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testHoldenAtsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := HoldenAts().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	holdenAtDBTypes = map[string]string{`ID`: `integer`, `HoldenAt`: `character varying`}
	_               = bytes.MinRead
)

func testHoldenAtsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(holdenAtPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(holdenAtAllColumns) == len(holdenAtPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testHoldenAtsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(holdenAtAllColumns) == len(holdenAtPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &HoldenAt{}
	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, holdenAtDBTypes, true, holdenAtPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(holdenAtAllColumns, holdenAtPrimaryKeyColumns) {
		fields = holdenAtAllColumns
	} else {
		fields = strmangle.SetComplement(
			holdenAtAllColumns,
			holdenAtPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := HoldenAtSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testHoldenAtsUpsert(t *testing.T) {
	t.Parallel()

	if len(holdenAtAllColumns) == len(holdenAtPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := HoldenAt{}
	if err = randomize.Struct(seed, &o, holdenAtDBTypes, true); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert HoldenAt: %s", err)
	}

	count, err := HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, holdenAtDBTypes, false, holdenAtPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize HoldenAt struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert HoldenAt: %s", err)
	}

	count, err = HoldenAts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
