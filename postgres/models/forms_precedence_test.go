// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testFormsPrecedences(t *testing.T) {
	t.Parallel()

	query := FormsPrecedences()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testFormsPrecedencesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testFormsPrecedencesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := FormsPrecedences().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testFormsPrecedencesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := FormsPrecedenceSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testFormsPrecedencesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := FormsPrecedenceExists(ctx, tx, o.UUID)
	if err != nil {
		t.Errorf("Unable to check if FormsPrecedence exists: %s", err)
	}
	if !e {
		t.Errorf("Expected FormsPrecedenceExists to return true, but got false.")
	}
}

func testFormsPrecedencesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	formsPrecedenceFound, err := FindFormsPrecedence(ctx, tx, o.UUID)
	if err != nil {
		t.Error(err)
	}

	if formsPrecedenceFound == nil {
		t.Error("want a record, got nil")
	}
}

func testFormsPrecedencesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = FormsPrecedences().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testFormsPrecedencesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := FormsPrecedences().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testFormsPrecedencesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	formsPrecedenceOne := &FormsPrecedence{}
	formsPrecedenceTwo := &FormsPrecedence{}
	if err = randomize.Struct(seed, formsPrecedenceOne, formsPrecedenceDBTypes, false, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}
	if err = randomize.Struct(seed, formsPrecedenceTwo, formsPrecedenceDBTypes, false, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = formsPrecedenceOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = formsPrecedenceTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := FormsPrecedences().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testFormsPrecedencesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	formsPrecedenceOne := &FormsPrecedence{}
	formsPrecedenceTwo := &FormsPrecedence{}
	if err = randomize.Struct(seed, formsPrecedenceOne, formsPrecedenceDBTypes, false, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}
	if err = randomize.Struct(seed, formsPrecedenceTwo, formsPrecedenceDBTypes, false, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = formsPrecedenceOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = formsPrecedenceTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testFormsPrecedencesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testFormsPrecedencesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(formsPrecedenceColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testFormsPrecedencesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testFormsPrecedencesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := FormsPrecedenceSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testFormsPrecedencesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := FormsPrecedences().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	formsPrecedenceDBTypes = map[string]string{`UUID`: `character`, `Category`: `text`, `Title`: `text`, `Content`: `text`, `VersionNo`: `integer`, `ID`: `integer`}
	_                      = bytes.MinRead
)

func testFormsPrecedencesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(formsPrecedencePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(formsPrecedenceAllColumns) == len(formsPrecedencePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedencePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testFormsPrecedencesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(formsPrecedenceAllColumns) == len(formsPrecedencePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &FormsPrecedence{}
	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedenceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, formsPrecedenceDBTypes, true, formsPrecedencePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(formsPrecedenceAllColumns, formsPrecedencePrimaryKeyColumns) {
		fields = formsPrecedenceAllColumns
	} else {
		fields = strmangle.SetComplement(
			formsPrecedenceAllColumns,
			formsPrecedencePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := FormsPrecedenceSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testFormsPrecedencesUpsert(t *testing.T) {
	t.Parallel()

	if len(formsPrecedenceAllColumns) == len(formsPrecedencePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := FormsPrecedence{}
	if err = randomize.Struct(seed, &o, formsPrecedenceDBTypes, true); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert FormsPrecedence: %s", err)
	}

	count, err := FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, formsPrecedenceDBTypes, false, formsPrecedencePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize FormsPrecedence struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert FormsPrecedence: %s", err)
	}

	count, err = FormsPrecedences().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
