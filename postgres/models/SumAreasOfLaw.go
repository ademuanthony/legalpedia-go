// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SumAreasOfLaw is an object representing the database table.
type SumAreasOfLaw struct {
	ID          int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	SuitNo      null.String `boil:"SuitNo" json:"SuitNo,omitempty" toml:"SuitNo" yaml:"SuitNo,omitempty"`
	AreaOfLawID null.Int    `boil:"AreaOfLawID" json:"AreaOfLawID,omitempty" toml:"AreaOfLawID" yaml:"AreaOfLawID,omitempty"`

	R *sumAreasOfLawR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L sumAreasOfLawL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SumAreasOfLawColumns = struct {
	ID          string
	SuitNo      string
	AreaOfLawID string
}{
	ID:          "Id",
	SuitNo:      "SuitNo",
	AreaOfLawID: "AreaOfLawID",
}

// Generated where

var SumAreasOfLawWhere = struct {
	ID          whereHelperint
	SuitNo      whereHelpernull_String
	AreaOfLawID whereHelpernull_Int
}{
	ID:          whereHelperint{field: "\"SumAreasOfLaw\".\"Id\""},
	SuitNo:      whereHelpernull_String{field: "\"SumAreasOfLaw\".\"SuitNo\""},
	AreaOfLawID: whereHelpernull_Int{field: "\"SumAreasOfLaw\".\"AreaOfLawID\""},
}

// SumAreasOfLawRels is where relationship names are stored.
var SumAreasOfLawRels = struct {
}{}

// sumAreasOfLawR is where relationships are stored.
type sumAreasOfLawR struct {
}

// NewStruct creates a new relationship struct
func (*sumAreasOfLawR) NewStruct() *sumAreasOfLawR {
	return &sumAreasOfLawR{}
}

// sumAreasOfLawL is where Load methods for each relationship are stored.
type sumAreasOfLawL struct{}

var (
	sumAreasOfLawAllColumns            = []string{"Id", "SuitNo", "AreaOfLawID"}
	sumAreasOfLawColumnsWithoutDefault = []string{"Id", "SuitNo", "AreaOfLawID"}
	sumAreasOfLawColumnsWithDefault    = []string{}
	sumAreasOfLawPrimaryKeyColumns     = []string{"Id"}
)

type (
	// SumAreasOfLawSlice is an alias for a slice of pointers to SumAreasOfLaw.
	// This should generally be used opposed to []SumAreasOfLaw.
	SumAreasOfLawSlice []*SumAreasOfLaw

	sumAreasOfLawQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	sumAreasOfLawType                 = reflect.TypeOf(&SumAreasOfLaw{})
	sumAreasOfLawMapping              = queries.MakeStructMapping(sumAreasOfLawType)
	sumAreasOfLawPrimaryKeyMapping, _ = queries.BindMapping(sumAreasOfLawType, sumAreasOfLawMapping, sumAreasOfLawPrimaryKeyColumns)
	sumAreasOfLawInsertCacheMut       sync.RWMutex
	sumAreasOfLawInsertCache          = make(map[string]insertCache)
	sumAreasOfLawUpdateCacheMut       sync.RWMutex
	sumAreasOfLawUpdateCache          = make(map[string]updateCache)
	sumAreasOfLawUpsertCacheMut       sync.RWMutex
	sumAreasOfLawUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single sumAreasOfLaw record from the query.
func (q sumAreasOfLawQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SumAreasOfLaw, error) {
	o := &SumAreasOfLaw{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for SumAreasOfLaw")
	}

	return o, nil
}

// All returns all SumAreasOfLaw records from the query.
func (q sumAreasOfLawQuery) All(ctx context.Context, exec boil.ContextExecutor) (SumAreasOfLawSlice, error) {
	var o []*SumAreasOfLaw

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SumAreasOfLaw slice")
	}

	return o, nil
}

// Count returns the count of all SumAreasOfLaw records in the query.
func (q sumAreasOfLawQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count SumAreasOfLaw rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q sumAreasOfLawQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if SumAreasOfLaw exists")
	}

	return count > 0, nil
}

// SumAreasOfLaws retrieves all the records using an executor.
func SumAreasOfLaws(mods ...qm.QueryMod) sumAreasOfLawQuery {
	mods = append(mods, qm.From("\"SumAreasOfLaw\""))
	return sumAreasOfLawQuery{NewQuery(mods...)}
}

// FindSumAreasOfLaw retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSumAreasOfLaw(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*SumAreasOfLaw, error) {
	sumAreasOfLawObj := &SumAreasOfLaw{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"SumAreasOfLaw\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, sumAreasOfLawObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from SumAreasOfLaw")
	}

	return sumAreasOfLawObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SumAreasOfLaw) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SumAreasOfLaw provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(sumAreasOfLawColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	sumAreasOfLawInsertCacheMut.RLock()
	cache, cached := sumAreasOfLawInsertCache[key]
	sumAreasOfLawInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			sumAreasOfLawAllColumns,
			sumAreasOfLawColumnsWithDefault,
			sumAreasOfLawColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(sumAreasOfLawType, sumAreasOfLawMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(sumAreasOfLawType, sumAreasOfLawMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"SumAreasOfLaw\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"SumAreasOfLaw\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into SumAreasOfLaw")
	}

	if !cached {
		sumAreasOfLawInsertCacheMut.Lock()
		sumAreasOfLawInsertCache[key] = cache
		sumAreasOfLawInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the SumAreasOfLaw.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SumAreasOfLaw) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	sumAreasOfLawUpdateCacheMut.RLock()
	cache, cached := sumAreasOfLawUpdateCache[key]
	sumAreasOfLawUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			sumAreasOfLawAllColumns,
			sumAreasOfLawPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update SumAreasOfLaw, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"SumAreasOfLaw\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, sumAreasOfLawPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(sumAreasOfLawType, sumAreasOfLawMapping, append(wl, sumAreasOfLawPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update SumAreasOfLaw row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for SumAreasOfLaw")
	}

	if !cached {
		sumAreasOfLawUpdateCacheMut.Lock()
		sumAreasOfLawUpdateCache[key] = cache
		sumAreasOfLawUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q sumAreasOfLawQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for SumAreasOfLaw")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for SumAreasOfLaw")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SumAreasOfLawSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sumAreasOfLawPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"SumAreasOfLaw\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, sumAreasOfLawPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in sumAreasOfLaw slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all sumAreasOfLaw")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SumAreasOfLaw) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SumAreasOfLaw provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(sumAreasOfLawColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	sumAreasOfLawUpsertCacheMut.RLock()
	cache, cached := sumAreasOfLawUpsertCache[key]
	sumAreasOfLawUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			sumAreasOfLawAllColumns,
			sumAreasOfLawColumnsWithDefault,
			sumAreasOfLawColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			sumAreasOfLawAllColumns,
			sumAreasOfLawPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert SumAreasOfLaw, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(sumAreasOfLawPrimaryKeyColumns))
			copy(conflict, sumAreasOfLawPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"SumAreasOfLaw\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(sumAreasOfLawType, sumAreasOfLawMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(sumAreasOfLawType, sumAreasOfLawMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert SumAreasOfLaw")
	}

	if !cached {
		sumAreasOfLawUpsertCacheMut.Lock()
		sumAreasOfLawUpsertCache[key] = cache
		sumAreasOfLawUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single SumAreasOfLaw record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SumAreasOfLaw) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SumAreasOfLaw provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), sumAreasOfLawPrimaryKeyMapping)
	sql := "DELETE FROM \"SumAreasOfLaw\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from SumAreasOfLaw")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for SumAreasOfLaw")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q sumAreasOfLawQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no sumAreasOfLawQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from SumAreasOfLaw")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SumAreasOfLaw")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SumAreasOfLawSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sumAreasOfLawPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"SumAreasOfLaw\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sumAreasOfLawPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from sumAreasOfLaw slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SumAreasOfLaw")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SumAreasOfLaw) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSumAreasOfLaw(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SumAreasOfLawSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SumAreasOfLawSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sumAreasOfLawPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"SumAreasOfLaw\".* FROM \"SumAreasOfLaw\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sumAreasOfLawPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SumAreasOfLawSlice")
	}

	*o = slice

	return nil
}

// SumAreasOfLawExists checks if the SumAreasOfLaw row exists.
func SumAreasOfLawExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"SumAreasOfLaw\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if SumAreasOfLaw exists")
	}

	return exists, nil
}
