// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testLawOfFedParts(t *testing.T) {
	t.Parallel()

	query := LawOfFedParts()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testLawOfFedPartsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testLawOfFedPartsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := LawOfFedParts().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testLawOfFedPartsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := LawOfFedPartSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testLawOfFedPartsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := LawOfFedPartExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if LawOfFedPart exists: %s", err)
	}
	if !e {
		t.Errorf("Expected LawOfFedPartExists to return true, but got false.")
	}
}

func testLawOfFedPartsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	lawOfFedPartFound, err := FindLawOfFedPart(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if lawOfFedPartFound == nil {
		t.Error("want a record, got nil")
	}
}

func testLawOfFedPartsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = LawOfFedParts().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testLawOfFedPartsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := LawOfFedParts().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testLawOfFedPartsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	lawOfFedPartOne := &LawOfFedPart{}
	lawOfFedPartTwo := &LawOfFedPart{}
	if err = randomize.Struct(seed, lawOfFedPartOne, lawOfFedPartDBTypes, false, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}
	if err = randomize.Struct(seed, lawOfFedPartTwo, lawOfFedPartDBTypes, false, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = lawOfFedPartOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = lawOfFedPartTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := LawOfFedParts().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testLawOfFedPartsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	lawOfFedPartOne := &LawOfFedPart{}
	lawOfFedPartTwo := &LawOfFedPart{}
	if err = randomize.Struct(seed, lawOfFedPartOne, lawOfFedPartDBTypes, false, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}
	if err = randomize.Struct(seed, lawOfFedPartTwo, lawOfFedPartDBTypes, false, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = lawOfFedPartOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = lawOfFedPartTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testLawOfFedPartsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testLawOfFedPartsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(lawOfFedPartColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testLawOfFedPartsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testLawOfFedPartsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := LawOfFedPartSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testLawOfFedPartsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := LawOfFedParts().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	lawOfFedPartDBTypes = map[string]string{`ID`: `integer`, `PartHeader`: `character varying`, `LawId`: `integer`}
	_                   = bytes.MinRead
)

func testLawOfFedPartsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(lawOfFedPartPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(lawOfFedPartAllColumns) == len(lawOfFedPartPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testLawOfFedPartsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(lawOfFedPartAllColumns) == len(lawOfFedPartPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &LawOfFedPart{}
	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, lawOfFedPartDBTypes, true, lawOfFedPartPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(lawOfFedPartAllColumns, lawOfFedPartPrimaryKeyColumns) {
		fields = lawOfFedPartAllColumns
	} else {
		fields = strmangle.SetComplement(
			lawOfFedPartAllColumns,
			lawOfFedPartPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := LawOfFedPartSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testLawOfFedPartsUpsert(t *testing.T) {
	t.Parallel()

	if len(lawOfFedPartAllColumns) == len(lawOfFedPartPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := LawOfFedPart{}
	if err = randomize.Struct(seed, &o, lawOfFedPartDBTypes, true); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert LawOfFedPart: %s", err)
	}

	count, err := LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, lawOfFedPartDBTypes, false, lawOfFedPartPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize LawOfFedPart struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert LawOfFedPart: %s", err)
	}

	count, err = LawOfFedParts().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
