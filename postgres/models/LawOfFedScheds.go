// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// LawOfFedSched is an object representing the database table.
type LawOfFedSched struct {
	ID          int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	SchedHeader null.String `boil:"SchedHeader" json:"SchedHeader,omitempty" toml:"SchedHeader" yaml:"SchedHeader,omitempty"`
	SchedBody   null.String `boil:"SchedBody" json:"SchedBody,omitempty" toml:"SchedBody" yaml:"SchedBody,omitempty"`
	LawId       null.Int    `boil:"LawId" json:"LawId,omitempty" toml:"LawId" yaml:"LawId,omitempty"`

	R *lawOfFedSchedR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lawOfFedSchedL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LawOfFedSchedColumns = struct {
	ID          string
	SchedHeader string
	SchedBody   string
	LawId       string
}{
	ID:          "Id",
	SchedHeader: "SchedHeader",
	SchedBody:   "SchedBody",
	LawId:       "LawId",
}

// Generated where

var LawOfFedSchedWhere = struct {
	ID          whereHelperint
	SchedHeader whereHelpernull_String
	SchedBody   whereHelpernull_String
	LawId       whereHelpernull_Int
}{
	ID:          whereHelperint{field: "\"LawOfFedScheds\".\"Id\""},
	SchedHeader: whereHelpernull_String{field: "\"LawOfFedScheds\".\"SchedHeader\""},
	SchedBody:   whereHelpernull_String{field: "\"LawOfFedScheds\".\"SchedBody\""},
	LawId:       whereHelpernull_Int{field: "\"LawOfFedScheds\".\"LawId\""},
}

// LawOfFedSchedRels is where relationship names are stored.
var LawOfFedSchedRels = struct {
}{}

// lawOfFedSchedR is where relationships are stored.
type lawOfFedSchedR struct {
}

// NewStruct creates a new relationship struct
func (*lawOfFedSchedR) NewStruct() *lawOfFedSchedR {
	return &lawOfFedSchedR{}
}

// lawOfFedSchedL is where Load methods for each relationship are stored.
type lawOfFedSchedL struct{}

var (
	lawOfFedSchedAllColumns            = []string{"Id", "SchedHeader", "SchedBody", "LawId"}
	lawOfFedSchedColumnsWithoutDefault = []string{"Id", "SchedHeader", "SchedBody", "LawId"}
	lawOfFedSchedColumnsWithDefault    = []string{}
	lawOfFedSchedPrimaryKeyColumns     = []string{"Id"}
)

type (
	// LawOfFedSchedSlice is an alias for a slice of pointers to LawOfFedSched.
	// This should generally be used opposed to []LawOfFedSched.
	LawOfFedSchedSlice []*LawOfFedSched

	lawOfFedSchedQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lawOfFedSchedType                 = reflect.TypeOf(&LawOfFedSched{})
	lawOfFedSchedMapping              = queries.MakeStructMapping(lawOfFedSchedType)
	lawOfFedSchedPrimaryKeyMapping, _ = queries.BindMapping(lawOfFedSchedType, lawOfFedSchedMapping, lawOfFedSchedPrimaryKeyColumns)
	lawOfFedSchedInsertCacheMut       sync.RWMutex
	lawOfFedSchedInsertCache          = make(map[string]insertCache)
	lawOfFedSchedUpdateCacheMut       sync.RWMutex
	lawOfFedSchedUpdateCache          = make(map[string]updateCache)
	lawOfFedSchedUpsertCacheMut       sync.RWMutex
	lawOfFedSchedUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single lawOfFedSched record from the query.
func (q lawOfFedSchedQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LawOfFedSched, error) {
	o := &LawOfFedSched{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for LawOfFedScheds")
	}

	return o, nil
}

// All returns all LawOfFedSched records from the query.
func (q lawOfFedSchedQuery) All(ctx context.Context, exec boil.ContextExecutor) (LawOfFedSchedSlice, error) {
	var o []*LawOfFedSched

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to LawOfFedSched slice")
	}

	return o, nil
}

// Count returns the count of all LawOfFedSched records in the query.
func (q lawOfFedSchedQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count LawOfFedScheds rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lawOfFedSchedQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if LawOfFedScheds exists")
	}

	return count > 0, nil
}

// LawOfFedScheds retrieves all the records using an executor.
func LawOfFedScheds(mods ...qm.QueryMod) lawOfFedSchedQuery {
	mods = append(mods, qm.From("\"LawOfFedScheds\""))
	return lawOfFedSchedQuery{NewQuery(mods...)}
}

// FindLawOfFedSched retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLawOfFedSched(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*LawOfFedSched, error) {
	lawOfFedSchedObj := &LawOfFedSched{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"LawOfFedScheds\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lawOfFedSchedObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from LawOfFedScheds")
	}

	return lawOfFedSchedObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LawOfFedSched) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no LawOfFedScheds provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(lawOfFedSchedColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lawOfFedSchedInsertCacheMut.RLock()
	cache, cached := lawOfFedSchedInsertCache[key]
	lawOfFedSchedInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lawOfFedSchedAllColumns,
			lawOfFedSchedColumnsWithDefault,
			lawOfFedSchedColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lawOfFedSchedType, lawOfFedSchedMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lawOfFedSchedType, lawOfFedSchedMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"LawOfFedScheds\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"LawOfFedScheds\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into LawOfFedScheds")
	}

	if !cached {
		lawOfFedSchedInsertCacheMut.Lock()
		lawOfFedSchedInsertCache[key] = cache
		lawOfFedSchedInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the LawOfFedSched.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LawOfFedSched) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	lawOfFedSchedUpdateCacheMut.RLock()
	cache, cached := lawOfFedSchedUpdateCache[key]
	lawOfFedSchedUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lawOfFedSchedAllColumns,
			lawOfFedSchedPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update LawOfFedScheds, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"LawOfFedScheds\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, lawOfFedSchedPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lawOfFedSchedType, lawOfFedSchedMapping, append(wl, lawOfFedSchedPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update LawOfFedScheds row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for LawOfFedScheds")
	}

	if !cached {
		lawOfFedSchedUpdateCacheMut.Lock()
		lawOfFedSchedUpdateCache[key] = cache
		lawOfFedSchedUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q lawOfFedSchedQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for LawOfFedScheds")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for LawOfFedScheds")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LawOfFedSchedSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawOfFedSchedPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"LawOfFedScheds\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, lawOfFedSchedPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in lawOfFedSched slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all lawOfFedSched")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LawOfFedSched) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no LawOfFedScheds provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(lawOfFedSchedColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lawOfFedSchedUpsertCacheMut.RLock()
	cache, cached := lawOfFedSchedUpsertCache[key]
	lawOfFedSchedUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			lawOfFedSchedAllColumns,
			lawOfFedSchedColumnsWithDefault,
			lawOfFedSchedColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			lawOfFedSchedAllColumns,
			lawOfFedSchedPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert LawOfFedScheds, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(lawOfFedSchedPrimaryKeyColumns))
			copy(conflict, lawOfFedSchedPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"LawOfFedScheds\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(lawOfFedSchedType, lawOfFedSchedMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lawOfFedSchedType, lawOfFedSchedMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert LawOfFedScheds")
	}

	if !cached {
		lawOfFedSchedUpsertCacheMut.Lock()
		lawOfFedSchedUpsertCache[key] = cache
		lawOfFedSchedUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single LawOfFedSched record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LawOfFedSched) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no LawOfFedSched provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lawOfFedSchedPrimaryKeyMapping)
	sql := "DELETE FROM \"LawOfFedScheds\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from LawOfFedScheds")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for LawOfFedScheds")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lawOfFedSchedQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no lawOfFedSchedQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from LawOfFedScheds")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for LawOfFedScheds")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LawOfFedSchedSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawOfFedSchedPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"LawOfFedScheds\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lawOfFedSchedPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lawOfFedSched slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for LawOfFedScheds")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LawOfFedSched) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLawOfFedSched(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LawOfFedSchedSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LawOfFedSchedSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lawOfFedSchedPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"LawOfFedScheds\".* FROM \"LawOfFedScheds\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lawOfFedSchedPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in LawOfFedSchedSlice")
	}

	*o = slice

	return nil
}

// LawOfFedSchedExists checks if the LawOfFedSched row exists.
func LawOfFedSchedExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"LawOfFedScheds\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if LawOfFedScheds exists")
	}

	return exists, nil
}
