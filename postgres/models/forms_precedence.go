// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// FormsPrecedence is an object representing the database table.
type FormsPrecedence struct {
	UUID      string      `boil:"uuid" json:"uuid" toml:"uuid" yaml:"uuid"`
	Category  null.String `boil:"category" json:"category,omitempty" toml:"category" yaml:"category,omitempty"`
	Title     null.String `boil:"title" json:"title,omitempty" toml:"title" yaml:"title,omitempty"`
	Content   null.String `boil:"content" json:"content,omitempty" toml:"content" yaml:"content,omitempty"`
	VersionNo null.Int    `boil:"version_no" json:"version_no,omitempty" toml:"version_no" yaml:"version_no,omitempty"`
	ID        null.Int    `boil:"Id" json:"Id,omitempty" toml:"Id" yaml:"Id,omitempty"`

	R *formsPrecedenceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L formsPrecedenceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FormsPrecedenceColumns = struct {
	UUID      string
	Category  string
	Title     string
	Content   string
	VersionNo string
	ID        string
}{
	UUID:      "uuid",
	Category:  "category",
	Title:     "title",
	Content:   "content",
	VersionNo: "version_no",
	ID:        "Id",
}

// Generated where

var FormsPrecedenceWhere = struct {
	UUID      whereHelperstring
	Category  whereHelpernull_String
	Title     whereHelpernull_String
	Content   whereHelpernull_String
	VersionNo whereHelpernull_Int
	ID        whereHelpernull_Int
}{
	UUID:      whereHelperstring{field: "\"forms_precedence\".\"uuid\""},
	Category:  whereHelpernull_String{field: "\"forms_precedence\".\"category\""},
	Title:     whereHelpernull_String{field: "\"forms_precedence\".\"title\""},
	Content:   whereHelpernull_String{field: "\"forms_precedence\".\"content\""},
	VersionNo: whereHelpernull_Int{field: "\"forms_precedence\".\"version_no\""},
	ID:        whereHelpernull_Int{field: "\"forms_precedence\".\"Id\""},
}

// FormsPrecedenceRels is where relationship names are stored.
var FormsPrecedenceRels = struct {
}{}

// formsPrecedenceR is where relationships are stored.
type formsPrecedenceR struct {
}

// NewStruct creates a new relationship struct
func (*formsPrecedenceR) NewStruct() *formsPrecedenceR {
	return &formsPrecedenceR{}
}

// formsPrecedenceL is where Load methods for each relationship are stored.
type formsPrecedenceL struct{}

var (
	formsPrecedenceAllColumns            = []string{"uuid", "category", "title", "content", "version_no", "Id"}
	formsPrecedenceColumnsWithoutDefault = []string{"uuid", "category", "title", "content", "version_no", "Id"}
	formsPrecedenceColumnsWithDefault    = []string{}
	formsPrecedencePrimaryKeyColumns     = []string{"uuid"}
)

type (
	// FormsPrecedenceSlice is an alias for a slice of pointers to FormsPrecedence.
	// This should generally be used opposed to []FormsPrecedence.
	FormsPrecedenceSlice []*FormsPrecedence

	formsPrecedenceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	formsPrecedenceType                 = reflect.TypeOf(&FormsPrecedence{})
	formsPrecedenceMapping              = queries.MakeStructMapping(formsPrecedenceType)
	formsPrecedencePrimaryKeyMapping, _ = queries.BindMapping(formsPrecedenceType, formsPrecedenceMapping, formsPrecedencePrimaryKeyColumns)
	formsPrecedenceInsertCacheMut       sync.RWMutex
	formsPrecedenceInsertCache          = make(map[string]insertCache)
	formsPrecedenceUpdateCacheMut       sync.RWMutex
	formsPrecedenceUpdateCache          = make(map[string]updateCache)
	formsPrecedenceUpsertCacheMut       sync.RWMutex
	formsPrecedenceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single formsPrecedence record from the query.
func (q formsPrecedenceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*FormsPrecedence, error) {
	o := &FormsPrecedence{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for forms_precedence")
	}

	return o, nil
}

// All returns all FormsPrecedence records from the query.
func (q formsPrecedenceQuery) All(ctx context.Context, exec boil.ContextExecutor) (FormsPrecedenceSlice, error) {
	var o []*FormsPrecedence

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to FormsPrecedence slice")
	}

	return o, nil
}

// Count returns the count of all FormsPrecedence records in the query.
func (q formsPrecedenceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count forms_precedence rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q formsPrecedenceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if forms_precedence exists")
	}

	return count > 0, nil
}

// FormsPrecedences retrieves all the records using an executor.
func FormsPrecedences(mods ...qm.QueryMod) formsPrecedenceQuery {
	mods = append(mods, qm.From("\"forms_precedence\""))
	return formsPrecedenceQuery{NewQuery(mods...)}
}

// FindFormsPrecedence retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFormsPrecedence(ctx context.Context, exec boil.ContextExecutor, uUID string, selectCols ...string) (*FormsPrecedence, error) {
	formsPrecedenceObj := &FormsPrecedence{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"forms_precedence\" where \"uuid\"=$1", sel,
	)

	q := queries.Raw(query, uUID)

	err := q.Bind(ctx, exec, formsPrecedenceObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from forms_precedence")
	}

	return formsPrecedenceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FormsPrecedence) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no forms_precedence provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(formsPrecedenceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	formsPrecedenceInsertCacheMut.RLock()
	cache, cached := formsPrecedenceInsertCache[key]
	formsPrecedenceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			formsPrecedenceAllColumns,
			formsPrecedenceColumnsWithDefault,
			formsPrecedenceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(formsPrecedenceType, formsPrecedenceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(formsPrecedenceType, formsPrecedenceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"forms_precedence\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"forms_precedence\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into forms_precedence")
	}

	if !cached {
		formsPrecedenceInsertCacheMut.Lock()
		formsPrecedenceInsertCache[key] = cache
		formsPrecedenceInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the FormsPrecedence.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FormsPrecedence) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	formsPrecedenceUpdateCacheMut.RLock()
	cache, cached := formsPrecedenceUpdateCache[key]
	formsPrecedenceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			formsPrecedenceAllColumns,
			formsPrecedencePrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update forms_precedence, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"forms_precedence\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, formsPrecedencePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(formsPrecedenceType, formsPrecedenceMapping, append(wl, formsPrecedencePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update forms_precedence row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for forms_precedence")
	}

	if !cached {
		formsPrecedenceUpdateCacheMut.Lock()
		formsPrecedenceUpdateCache[key] = cache
		formsPrecedenceUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q formsPrecedenceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for forms_precedence")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for forms_precedence")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FormsPrecedenceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), formsPrecedencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"forms_precedence\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, formsPrecedencePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in formsPrecedence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all formsPrecedence")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FormsPrecedence) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no forms_precedence provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(formsPrecedenceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	formsPrecedenceUpsertCacheMut.RLock()
	cache, cached := formsPrecedenceUpsertCache[key]
	formsPrecedenceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			formsPrecedenceAllColumns,
			formsPrecedenceColumnsWithDefault,
			formsPrecedenceColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			formsPrecedenceAllColumns,
			formsPrecedencePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert forms_precedence, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(formsPrecedencePrimaryKeyColumns))
			copy(conflict, formsPrecedencePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"forms_precedence\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(formsPrecedenceType, formsPrecedenceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(formsPrecedenceType, formsPrecedenceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert forms_precedence")
	}

	if !cached {
		formsPrecedenceUpsertCacheMut.Lock()
		formsPrecedenceUpsertCache[key] = cache
		formsPrecedenceUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single FormsPrecedence record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FormsPrecedence) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no FormsPrecedence provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), formsPrecedencePrimaryKeyMapping)
	sql := "DELETE FROM \"forms_precedence\" WHERE \"uuid\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from forms_precedence")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for forms_precedence")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q formsPrecedenceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no formsPrecedenceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from forms_precedence")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for forms_precedence")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FormsPrecedenceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), formsPrecedencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"forms_precedence\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, formsPrecedencePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from formsPrecedence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for forms_precedence")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FormsPrecedence) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFormsPrecedence(ctx, exec, o.UUID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FormsPrecedenceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FormsPrecedenceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), formsPrecedencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"forms_precedence\".* FROM \"forms_precedence\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, formsPrecedencePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in FormsPrecedenceSlice")
	}

	*o = slice

	return nil
}

// FormsPrecedenceExists checks if the FormsPrecedence row exists.
func FormsPrecedenceExists(ctx context.Context, exec boil.ContextExecutor, uUID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"forms_precedence\" where \"uuid\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, uUID)
	}
	row := exec.QueryRowContext(ctx, sql, uUID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if forms_precedence exists")
	}

	return exists, nil
}
