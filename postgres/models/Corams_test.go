// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testCorams(t *testing.T) {
	t.Parallel()

	query := Corams()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testCoramsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCoramsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Corams().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCoramsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CoramSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCoramsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := CoramExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Coram exists: %s", err)
	}
	if !e {
		t.Errorf("Expected CoramExists to return true, but got false.")
	}
}

func testCoramsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	coramFound, err := FindCoram(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if coramFound == nil {
		t.Error("want a record, got nil")
	}
}

func testCoramsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Corams().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testCoramsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Corams().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testCoramsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	coramOne := &Coram{}
	coramTwo := &Coram{}
	if err = randomize.Struct(seed, coramOne, coramDBTypes, false, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}
	if err = randomize.Struct(seed, coramTwo, coramDBTypes, false, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = coramOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = coramTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Corams().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testCoramsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	coramOne := &Coram{}
	coramTwo := &Coram{}
	if err = randomize.Struct(seed, coramOne, coramDBTypes, false, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}
	if err = randomize.Struct(seed, coramTwo, coramDBTypes, false, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = coramOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = coramTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testCoramsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCoramsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(coramColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCoramsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCoramsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CoramSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCoramsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Corams().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	coramDBTypes = map[string]string{`ID`: `integer`, `Name`: `character varying`, `EmailAddr`: `character varying`, `PhoneNo`: `character varying`, `Version`: `timestamp without time zone`}
	_            = bytes.MinRead
)

func testCoramsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(coramPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(coramAllColumns) == len(coramPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, coramDBTypes, true, coramPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testCoramsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(coramAllColumns) == len(coramPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Coram{}
	if err = randomize.Struct(seed, o, coramDBTypes, true, coramColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, coramDBTypes, true, coramPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(coramAllColumns, coramPrimaryKeyColumns) {
		fields = coramAllColumns
	} else {
		fields = strmangle.SetComplement(
			coramAllColumns,
			coramPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := CoramSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testCoramsUpsert(t *testing.T) {
	t.Parallel()

	if len(coramAllColumns) == len(coramPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Coram{}
	if err = randomize.Struct(seed, &o, coramDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Coram: %s", err)
	}

	count, err := Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, coramDBTypes, false, coramPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Coram struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Coram: %s", err)
	}

	count, err = Corams().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
