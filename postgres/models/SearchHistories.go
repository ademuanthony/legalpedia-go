// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SearchHistory is an object representing the database table.
type SearchHistory struct {
	ID                   int         `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	SearchWord           null.String `boil:"SearchWord" json:"SearchWord,omitempty" toml:"SearchWord" yaml:"SearchWord,omitempty"`
	LastModificationTime null.Time   `boil:"LastModificationTime" json:"LastModificationTime,omitempty" toml:"LastModificationTime" yaml:"LastModificationTime,omitempty"`
	LastModifierUserId   null.Int64  `boil:"LastModifierUserId" json:"LastModifierUserId,omitempty" toml:"LastModifierUserId" yaml:"LastModifierUserId,omitempty"`
	CreationTime         null.Time   `boil:"CreationTime" json:"CreationTime,omitempty" toml:"CreationTime" yaml:"CreationTime,omitempty"`
	CreatorUserId        null.Int64  `boil:"CreatorUserId" json:"CreatorUserId,omitempty" toml:"CreatorUserId" yaml:"CreatorUserId,omitempty"`

	R *searchHistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L searchHistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SearchHistoryColumns = struct {
	ID                   string
	SearchWord           string
	LastModificationTime string
	LastModifierUserId   string
	CreationTime         string
	CreatorUserId        string
}{
	ID:                   "Id",
	SearchWord:           "SearchWord",
	LastModificationTime: "LastModificationTime",
	LastModifierUserId:   "LastModifierUserId",
	CreationTime:         "CreationTime",
	CreatorUserId:        "CreatorUserId",
}

// Generated where

var SearchHistoryWhere = struct {
	ID                   whereHelperint
	SearchWord           whereHelpernull_String
	LastModificationTime whereHelpernull_Time
	LastModifierUserId   whereHelpernull_Int64
	CreationTime         whereHelpernull_Time
	CreatorUserId        whereHelpernull_Int64
}{
	ID:                   whereHelperint{field: "\"SearchHistories\".\"Id\""},
	SearchWord:           whereHelpernull_String{field: "\"SearchHistories\".\"SearchWord\""},
	LastModificationTime: whereHelpernull_Time{field: "\"SearchHistories\".\"LastModificationTime\""},
	LastModifierUserId:   whereHelpernull_Int64{field: "\"SearchHistories\".\"LastModifierUserId\""},
	CreationTime:         whereHelpernull_Time{field: "\"SearchHistories\".\"CreationTime\""},
	CreatorUserId:        whereHelpernull_Int64{field: "\"SearchHistories\".\"CreatorUserId\""},
}

// SearchHistoryRels is where relationship names are stored.
var SearchHistoryRels = struct {
}{}

// searchHistoryR is where relationships are stored.
type searchHistoryR struct {
}

// NewStruct creates a new relationship struct
func (*searchHistoryR) NewStruct() *searchHistoryR {
	return &searchHistoryR{}
}

// searchHistoryL is where Load methods for each relationship are stored.
type searchHistoryL struct{}

var (
	searchHistoryAllColumns            = []string{"Id", "SearchWord", "LastModificationTime", "LastModifierUserId", "CreationTime", "CreatorUserId"}
	searchHistoryColumnsWithoutDefault = []string{"SearchWord", "LastModificationTime", "LastModifierUserId", "CreationTime", "CreatorUserId"}
	searchHistoryColumnsWithDefault    = []string{"Id"}
	searchHistoryPrimaryKeyColumns     = []string{"Id"}
)

type (
	// SearchHistorySlice is an alias for a slice of pointers to SearchHistory.
	// This should generally be used opposed to []SearchHistory.
	SearchHistorySlice []*SearchHistory

	searchHistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	searchHistoryType                 = reflect.TypeOf(&SearchHistory{})
	searchHistoryMapping              = queries.MakeStructMapping(searchHistoryType)
	searchHistoryPrimaryKeyMapping, _ = queries.BindMapping(searchHistoryType, searchHistoryMapping, searchHistoryPrimaryKeyColumns)
	searchHistoryInsertCacheMut       sync.RWMutex
	searchHistoryInsertCache          = make(map[string]insertCache)
	searchHistoryUpdateCacheMut       sync.RWMutex
	searchHistoryUpdateCache          = make(map[string]updateCache)
	searchHistoryUpsertCacheMut       sync.RWMutex
	searchHistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single searchHistory record from the query.
func (q searchHistoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SearchHistory, error) {
	o := &SearchHistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for SearchHistories")
	}

	return o, nil
}

// All returns all SearchHistory records from the query.
func (q searchHistoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (SearchHistorySlice, error) {
	var o []*SearchHistory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SearchHistory slice")
	}

	return o, nil
}

// Count returns the count of all SearchHistory records in the query.
func (q searchHistoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count SearchHistories rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q searchHistoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if SearchHistories exists")
	}

	return count > 0, nil
}

// SearchHistories retrieves all the records using an executor.
func SearchHistories(mods ...qm.QueryMod) searchHistoryQuery {
	mods = append(mods, qm.From("\"SearchHistories\""))
	return searchHistoryQuery{NewQuery(mods...)}
}

// FindSearchHistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSearchHistory(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*SearchHistory, error) {
	searchHistoryObj := &SearchHistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"SearchHistories\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, searchHistoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from SearchHistories")
	}

	return searchHistoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SearchHistory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SearchHistories provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(searchHistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	searchHistoryInsertCacheMut.RLock()
	cache, cached := searchHistoryInsertCache[key]
	searchHistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			searchHistoryAllColumns,
			searchHistoryColumnsWithDefault,
			searchHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(searchHistoryType, searchHistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(searchHistoryType, searchHistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"SearchHistories\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"SearchHistories\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into SearchHistories")
	}

	if !cached {
		searchHistoryInsertCacheMut.Lock()
		searchHistoryInsertCache[key] = cache
		searchHistoryInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the SearchHistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SearchHistory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	searchHistoryUpdateCacheMut.RLock()
	cache, cached := searchHistoryUpdateCache[key]
	searchHistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			searchHistoryAllColumns,
			searchHistoryPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update SearchHistories, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"SearchHistories\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, searchHistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(searchHistoryType, searchHistoryMapping, append(wl, searchHistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update SearchHistories row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for SearchHistories")
	}

	if !cached {
		searchHistoryUpdateCacheMut.Lock()
		searchHistoryUpdateCache[key] = cache
		searchHistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q searchHistoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for SearchHistories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for SearchHistories")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SearchHistorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), searchHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"SearchHistories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, searchHistoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in searchHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all searchHistory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SearchHistory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SearchHistories provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(searchHistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	searchHistoryUpsertCacheMut.RLock()
	cache, cached := searchHistoryUpsertCache[key]
	searchHistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			searchHistoryAllColumns,
			searchHistoryColumnsWithDefault,
			searchHistoryColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			searchHistoryAllColumns,
			searchHistoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert SearchHistories, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(searchHistoryPrimaryKeyColumns))
			copy(conflict, searchHistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"SearchHistories\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(searchHistoryType, searchHistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(searchHistoryType, searchHistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert SearchHistories")
	}

	if !cached {
		searchHistoryUpsertCacheMut.Lock()
		searchHistoryUpsertCache[key] = cache
		searchHistoryUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single SearchHistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SearchHistory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SearchHistory provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), searchHistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"SearchHistories\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from SearchHistories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for SearchHistories")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q searchHistoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no searchHistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from SearchHistories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SearchHistories")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SearchHistorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), searchHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"SearchHistories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, searchHistoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from searchHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SearchHistories")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SearchHistory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSearchHistory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SearchHistorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SearchHistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), searchHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"SearchHistories\".* FROM \"SearchHistories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, searchHistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SearchHistorySlice")
	}

	*o = slice

	return nil
}

// SearchHistoryExists checks if the SearchHistory row exists.
func SearchHistoryExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"SearchHistories\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if SearchHistories exists")
	}

	return exists, nil
}
