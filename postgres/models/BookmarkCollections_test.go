// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBookmarkCollections(t *testing.T) {
	t.Parallel()

	query := BookmarkCollections()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBookmarkCollectionsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBookmarkCollectionsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := BookmarkCollections().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBookmarkCollectionsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BookmarkCollectionSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBookmarkCollectionsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BookmarkCollectionExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if BookmarkCollection exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BookmarkCollectionExists to return true, but got false.")
	}
}

func testBookmarkCollectionsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	bookmarkCollectionFound, err := FindBookmarkCollection(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if bookmarkCollectionFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBookmarkCollectionsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = BookmarkCollections().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testBookmarkCollectionsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := BookmarkCollections().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBookmarkCollectionsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	bookmarkCollectionOne := &BookmarkCollection{}
	bookmarkCollectionTwo := &BookmarkCollection{}
	if err = randomize.Struct(seed, bookmarkCollectionOne, bookmarkCollectionDBTypes, false, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}
	if err = randomize.Struct(seed, bookmarkCollectionTwo, bookmarkCollectionDBTypes, false, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = bookmarkCollectionOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = bookmarkCollectionTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := BookmarkCollections().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBookmarkCollectionsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	bookmarkCollectionOne := &BookmarkCollection{}
	bookmarkCollectionTwo := &BookmarkCollection{}
	if err = randomize.Struct(seed, bookmarkCollectionOne, bookmarkCollectionDBTypes, false, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}
	if err = randomize.Struct(seed, bookmarkCollectionTwo, bookmarkCollectionDBTypes, false, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = bookmarkCollectionOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = bookmarkCollectionTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testBookmarkCollectionsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBookmarkCollectionsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(bookmarkCollectionColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBookmarkCollectionsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBookmarkCollectionsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BookmarkCollectionSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBookmarkCollectionsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := BookmarkCollections().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	bookmarkCollectionDBTypes = map[string]string{`ID`: `text`, `Name`: `text`, `UserId`: `bigint`}
	_                         = bytes.MinRead
)

func testBookmarkCollectionsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(bookmarkCollectionPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(bookmarkCollectionAllColumns) == len(bookmarkCollectionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBookmarkCollectionsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(bookmarkCollectionAllColumns) == len(bookmarkCollectionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &BookmarkCollection{}
	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, bookmarkCollectionDBTypes, true, bookmarkCollectionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(bookmarkCollectionAllColumns, bookmarkCollectionPrimaryKeyColumns) {
		fields = bookmarkCollectionAllColumns
	} else {
		fields = strmangle.SetComplement(
			bookmarkCollectionAllColumns,
			bookmarkCollectionPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BookmarkCollectionSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBookmarkCollectionsUpsert(t *testing.T) {
	t.Parallel()

	if len(bookmarkCollectionAllColumns) == len(bookmarkCollectionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := BookmarkCollection{}
	if err = randomize.Struct(seed, &o, bookmarkCollectionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert BookmarkCollection: %s", err)
	}

	count, err := BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, bookmarkCollectionDBTypes, false, bookmarkCollectionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BookmarkCollection struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert BookmarkCollection: %s", err)
	}

	count, err = BookmarkCollections().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
