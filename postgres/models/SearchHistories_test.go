// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSearchHistories(t *testing.T) {
	t.Parallel()

	query := SearchHistories()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSearchHistoriesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSearchHistoriesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SearchHistories().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSearchHistoriesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SearchHistorySlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSearchHistoriesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SearchHistoryExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SearchHistory exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SearchHistoryExists to return true, but got false.")
	}
}

func testSearchHistoriesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	searchHistoryFound, err := FindSearchHistory(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if searchHistoryFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSearchHistoriesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SearchHistories().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testSearchHistoriesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SearchHistories().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSearchHistoriesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	searchHistoryOne := &SearchHistory{}
	searchHistoryTwo := &SearchHistory{}
	if err = randomize.Struct(seed, searchHistoryOne, searchHistoryDBTypes, false, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}
	if err = randomize.Struct(seed, searchHistoryTwo, searchHistoryDBTypes, false, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = searchHistoryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = searchHistoryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SearchHistories().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSearchHistoriesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	searchHistoryOne := &SearchHistory{}
	searchHistoryTwo := &SearchHistory{}
	if err = randomize.Struct(seed, searchHistoryOne, searchHistoryDBTypes, false, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}
	if err = randomize.Struct(seed, searchHistoryTwo, searchHistoryDBTypes, false, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = searchHistoryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = searchHistoryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSearchHistoriesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSearchHistoriesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(searchHistoryColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSearchHistoriesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSearchHistoriesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SearchHistorySlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSearchHistoriesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SearchHistories().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	searchHistoryDBTypes = map[string]string{`ID`: `integer`, `SearchWord`: `text`, `LastModificationTime`: `timestamp without time zone`, `LastModifierUserId`: `bigint`, `CreationTime`: `timestamp without time zone`, `CreatorUserId`: `bigint`}
	_                    = bytes.MinRead
)

func testSearchHistoriesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(searchHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(searchHistoryAllColumns) == len(searchHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSearchHistoriesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(searchHistoryAllColumns) == len(searchHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SearchHistory{}
	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, searchHistoryDBTypes, true, searchHistoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(searchHistoryAllColumns, searchHistoryPrimaryKeyColumns) {
		fields = searchHistoryAllColumns
	} else {
		fields = strmangle.SetComplement(
			searchHistoryAllColumns,
			searchHistoryPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SearchHistorySlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSearchHistoriesUpsert(t *testing.T) {
	t.Parallel()

	if len(searchHistoryAllColumns) == len(searchHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SearchHistory{}
	if err = randomize.Struct(seed, &o, searchHistoryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SearchHistory: %s", err)
	}

	count, err := SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, searchHistoryDBTypes, false, searchHistoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SearchHistory struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SearchHistory: %s", err)
	}

	count, err = SearchHistories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
