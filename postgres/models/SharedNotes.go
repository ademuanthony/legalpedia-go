// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SharedNote is an object representing the database table.
type SharedNote struct {
	ID           string      `boil:"Id" json:"Id" toml:"Id" yaml:"Id"`
	NoteId       null.String `boil:"NoteId" json:"NoteId,omitempty" toml:"NoteId" yaml:"NoteId,omitempty"`
	CreatedAt    time.Time   `boil:"CreatedAt" json:"CreatedAt" toml:"CreatedAt" yaml:"CreatedAt"`
	UserId       null.Int64  `boil:"UserId" json:"UserId,omitempty" toml:"UserId" yaml:"UserId,omitempty"`
	TargetUserId null.Int64  `boil:"TargetUserId" json:"TargetUserId,omitempty" toml:"TargetUserId" yaml:"TargetUserId,omitempty"`

	R *sharedNoteR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L sharedNoteL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SharedNoteColumns = struct {
	ID           string
	NoteId       string
	CreatedAt    string
	UserId       string
	TargetUserId string
}{
	ID:           "Id",
	NoteId:       "NoteId",
	CreatedAt:    "CreatedAt",
	UserId:       "UserId",
	TargetUserId: "TargetUserId",
}

// Generated where

var SharedNoteWhere = struct {
	ID           whereHelperstring
	NoteId       whereHelpernull_String
	CreatedAt    whereHelpertime_Time
	UserId       whereHelpernull_Int64
	TargetUserId whereHelpernull_Int64
}{
	ID:           whereHelperstring{field: "\"SharedNotes\".\"Id\""},
	NoteId:       whereHelpernull_String{field: "\"SharedNotes\".\"NoteId\""},
	CreatedAt:    whereHelpertime_Time{field: "\"SharedNotes\".\"CreatedAt\""},
	UserId:       whereHelpernull_Int64{field: "\"SharedNotes\".\"UserId\""},
	TargetUserId: whereHelpernull_Int64{field: "\"SharedNotes\".\"TargetUserId\""},
}

// SharedNoteRels is where relationship names are stored.
var SharedNoteRels = struct {
}{}

// sharedNoteR is where relationships are stored.
type sharedNoteR struct {
}

// NewStruct creates a new relationship struct
func (*sharedNoteR) NewStruct() *sharedNoteR {
	return &sharedNoteR{}
}

// sharedNoteL is where Load methods for each relationship are stored.
type sharedNoteL struct{}

var (
	sharedNoteAllColumns            = []string{"Id", "NoteId", "CreatedAt", "UserId", "TargetUserId"}
	sharedNoteColumnsWithoutDefault = []string{"Id", "NoteId", "CreatedAt", "UserId", "TargetUserId"}
	sharedNoteColumnsWithDefault    = []string{}
	sharedNotePrimaryKeyColumns     = []string{"Id"}
)

type (
	// SharedNoteSlice is an alias for a slice of pointers to SharedNote.
	// This should generally be used opposed to []SharedNote.
	SharedNoteSlice []*SharedNote

	sharedNoteQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	sharedNoteType                 = reflect.TypeOf(&SharedNote{})
	sharedNoteMapping              = queries.MakeStructMapping(sharedNoteType)
	sharedNotePrimaryKeyMapping, _ = queries.BindMapping(sharedNoteType, sharedNoteMapping, sharedNotePrimaryKeyColumns)
	sharedNoteInsertCacheMut       sync.RWMutex
	sharedNoteInsertCache          = make(map[string]insertCache)
	sharedNoteUpdateCacheMut       sync.RWMutex
	sharedNoteUpdateCache          = make(map[string]updateCache)
	sharedNoteUpsertCacheMut       sync.RWMutex
	sharedNoteUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single sharedNote record from the query.
func (q sharedNoteQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SharedNote, error) {
	o := &SharedNote{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for SharedNotes")
	}

	return o, nil
}

// All returns all SharedNote records from the query.
func (q sharedNoteQuery) All(ctx context.Context, exec boil.ContextExecutor) (SharedNoteSlice, error) {
	var o []*SharedNote

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SharedNote slice")
	}

	return o, nil
}

// Count returns the count of all SharedNote records in the query.
func (q sharedNoteQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count SharedNotes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q sharedNoteQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if SharedNotes exists")
	}

	return count > 0, nil
}

// SharedNotes retrieves all the records using an executor.
func SharedNotes(mods ...qm.QueryMod) sharedNoteQuery {
	mods = append(mods, qm.From("\"SharedNotes\""))
	return sharedNoteQuery{NewQuery(mods...)}
}

// FindSharedNote retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSharedNote(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SharedNote, error) {
	sharedNoteObj := &SharedNote{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"SharedNotes\" where \"Id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, sharedNoteObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from SharedNotes")
	}

	return sharedNoteObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SharedNote) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SharedNotes provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(sharedNoteColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	sharedNoteInsertCacheMut.RLock()
	cache, cached := sharedNoteInsertCache[key]
	sharedNoteInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			sharedNoteAllColumns,
			sharedNoteColumnsWithDefault,
			sharedNoteColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(sharedNoteType, sharedNoteMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(sharedNoteType, sharedNoteMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"SharedNotes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"SharedNotes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into SharedNotes")
	}

	if !cached {
		sharedNoteInsertCacheMut.Lock()
		sharedNoteInsertCache[key] = cache
		sharedNoteInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the SharedNote.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SharedNote) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	sharedNoteUpdateCacheMut.RLock()
	cache, cached := sharedNoteUpdateCache[key]
	sharedNoteUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			sharedNoteAllColumns,
			sharedNotePrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update SharedNotes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"SharedNotes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, sharedNotePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(sharedNoteType, sharedNoteMapping, append(wl, sharedNotePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update SharedNotes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for SharedNotes")
	}

	if !cached {
		sharedNoteUpdateCacheMut.Lock()
		sharedNoteUpdateCache[key] = cache
		sharedNoteUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q sharedNoteQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for SharedNotes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for SharedNotes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SharedNoteSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sharedNotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"SharedNotes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, sharedNotePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in sharedNote slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all sharedNote")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SharedNote) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no SharedNotes provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(sharedNoteColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	sharedNoteUpsertCacheMut.RLock()
	cache, cached := sharedNoteUpsertCache[key]
	sharedNoteUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			sharedNoteAllColumns,
			sharedNoteColumnsWithDefault,
			sharedNoteColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			sharedNoteAllColumns,
			sharedNotePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert SharedNotes, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(sharedNotePrimaryKeyColumns))
			copy(conflict, sharedNotePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"SharedNotes\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(sharedNoteType, sharedNoteMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(sharedNoteType, sharedNoteMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert SharedNotes")
	}

	if !cached {
		sharedNoteUpsertCacheMut.Lock()
		sharedNoteUpsertCache[key] = cache
		sharedNoteUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single SharedNote record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SharedNote) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SharedNote provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), sharedNotePrimaryKeyMapping)
	sql := "DELETE FROM \"SharedNotes\" WHERE \"Id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from SharedNotes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for SharedNotes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q sharedNoteQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no sharedNoteQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from SharedNotes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SharedNotes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SharedNoteSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sharedNotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"SharedNotes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sharedNotePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from sharedNote slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for SharedNotes")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SharedNote) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSharedNote(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SharedNoteSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SharedNoteSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sharedNotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"SharedNotes\".* FROM \"SharedNotes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sharedNotePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SharedNoteSlice")
	}

	*o = slice

	return nil
}

// SharedNoteExists checks if the SharedNote row exists.
func SharedNoteExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"SharedNotes\" where \"Id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if SharedNotes exists")
	}

	return exists, nil
}
